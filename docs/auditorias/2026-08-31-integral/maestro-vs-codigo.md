FORTALEZAS

1. **Los 13 commits citados existen y HEAD coincide con el estampado.** `git log`: S0.1 `40a45af`, S0.2 `79565b8`, S0.3 `18c7adf`, S0.4 `4e90539`, S0.5 `f70a10a`, S0.6 `2bf6630`, S0.7 `5d24463` (=HEAD), AUD-5 `edb1468`+`c4d47c6`, AUD-6 `f41f6cc`, PLAN-1 `787ec86`, CAT-1/2/3 `37c3221`/`fc9e9f9`/`4300890`. Ninguno falta, ninguno está fuera de orden.
2. **Las entregas están en el árbol, verificadas una a una.** 037/038/039 existen (`src/database/migrations/037_etiquetado_que_encarece.sql`, `038_entierro_s05.sql` — dropea exactamente las 6 tablas prometidas, líneas 38–43—, `039_idempotencia_persistida.sql`); `src/services/inventory/costing.ts` no existe (S0.4); `gateMutation` se consume en 13 archivos (`src/cli/kernel/risk.ts` + comandos); outbox partido (`src/cli/mnemosine.ts:1429`); superficie nombrada (`src/ai/tools/superficie.ts`); `ingest_auto_post` con 4 lectores reales (`src/ai/ingest-thresholds.ts`, `src/services/policy/policy-preview.ts`…); suelo en `docs/catalogo-minimos.json` (92/84).
3. **Los tres cerrojos de S0.1 están cableados en CI.** `.github/workflows/ci.yml:70` (`plan:status --exigir=` con exactamente los 9 paquetes verdes — AUD-6 sacó E1.2/E4.1 como prometía) y `:78` (`catalogo-estado.ts --check` contra el suelo).
4. **Los seis rojos nombrados SON el estado real.** `npm run plan:status` hoy: E1.2 2/3 (cfdi_classifications cero menciones en src), E1.3 0/1 (8 de 15 políticas — la cifra post-S0.3 que el plan predice), E1.4 1/2 (`runMonthlyDepreciation` sin llamador — confirmado: única referencia viva es `src/services/assets/depreciation.ts:270`), E4.1 2/3 (paycheck_taxes/employer_tax_liabilities/garnishments leídas y jamás escritas), E4.2 1/2 (las mismas 4 copias del SQL: external-service, resolvers GraphQL, reports.ts, blockchain/orchestrator), E5.1 5/6 (allDeclarations sin consumidor real). 9/15 verdes, exacto.
5. **Las correcciones de §2 se sostienen en el código.** `invoice send` ya responde `marked_sent`/`transmitted:false` con nota honesta (`src/api/rest/routes/invoices.ts:149-158`); `period open` está ✅ y viva en el catálogo; `rep reconcile` está 🟡 post-AUD-5; `rep_faltante_emitido/recibido` efectivamente nunca se crearon (0 menciones); el censo es criterio real con RECLAMADAS y dueño (`src/plan/criterios.ts:246-253`).
6. **Las cifras estructurales grandes casan.** Fase 2 = 972 y fase 3 = 273 exactas; 244 filas de fase 3 sin motor (el corte de S0.5); F02 = 42 (cfdi 20 · sat 17 · rep 5) exacto; F05 = 38 (bank 30 · reconciliation 8); F06 = 21; F07 = 10; F08 = 9; cola con 65 familias y 24 de un solo comando; pac 4 · filing 4 · diot 4 · tax-mailbox 2, todo exacto.
7. **Las puertas reales están en verde.** `tsc --noEmit` limpio; 2063 pruebas unitarias y 211 de integración pasan (crecieron desde las 2045+206 del plan — dirección correcta).

BRECHAS

1. **§1 está caduco en siete cifras a su PROPIO commit estampado (5d24463), y se contradice con su §4.** §1 dice 1 626 filas / 380 fase 1 / 82 tecleables / 90 invocables / 106 comandos / 40 violaciones / 2 045+206 pruebas; la medición de hoy (mismo HEAD) da 1 624 / 378 / 84 / 92 / 108 / 37 / 2 063+211. Evidencia: `jq length` del `--json` = 1624; `docs/cli-command-catalog.md:52,56` (108 comandos, 92 y 378/84); `LINEA_BASE` en `src/cli/kernel/audit.ts:197` tiene 37 entradas; suites arriba. El propio S0.7 del plan dice "1,624 filas" y "suelo 92/84" — §1 y §4 se contradicen dentro del mismo documento. Incluye el objetivo comprometible: plan dice 1,382, el medidor publica **1380** (`docs/cli-command-catalog.md:58`). Es violación de la regla del propio §7 ("ningún documento vuelve a fijar una cifra"). NUEVA.
2. **Tres recuentos de flujo y la cola no casan con el catálogo de hoy.** F01 = 31, no 32 (ledger fase 1 = 4, no 5); F03 = 37, no 38 (ar = 4, no 5); F04 = 11, no 12 (ap = 1, no 2); F09–F12 = **179** filas, no 175. Y sus "mayores" están mal: **report con 10 es la familia más grande de la cola y no aparece en la lista**, entity es 7 (no 6), trial-balance es 3 (no 5). Evidencia: agregados jq sobre `catalogo-estado.ts --json`. NUEVA.
3. **Una fila del catálogo tiene fase vacía y escapa a todo conteo.** `pac create` trae `fase: ""` (378+972+273+**1**=1624): pac tiene 5 filas y toda suma por fase ve 4. Ningún rector la nombra y `--check` no la atrapa. NUEVA.
4. **§7 promete "«doctor sin huérfanos nuevos» entra como criterio" y ese criterio no existe.** `src/plan/criterios.ts` sólo tiene el censo de TABLAS (línea 237); no hay criterio que corra el barrido de capacidad huérfana a nivel export. Los dos huérfanos que §3 censa siguen exactamente igual —`earlyPaymentDiscount` (`src/services/ap/bill-service.ts:570`, cero llamadores) y `calculateBenefitsForPaycheck` (`src/services/payroll/usa/benefits/benefits-service.ts:79`, cero llamadores)— sin trinquete que impida el siguiente. El plan nombra los huérfanos (F04/F08) pero el vigía prometido es NUEVA como brecha.
5. **Cuatro adaptadores de integración son capacidad de la clase recordInventorySale y el censo de §3 no los incluye.** `stripe-adapter`, `conekta-adapter`, `sendgrid-adapter` y `s3-adapter` se registran al importar (`src/services/integrations/index.ts:12-15`) y su único consumidor es la ruta de configurar/probar (`src/api/rest/routes/integrations.ts:24-119`): ningún camino de negocio los llama. El caso agudo: F03 plantea "cablear el envío o retirar la promesa" de invoice send **sin mencionar que un adaptador SendGrid ya existe** (88 líneas, cero usos). NUEVA.
6. **§5 subdeclara la superficie PAC.** Dice "el adaptador de Sovos Reachcore está escrito"; en el árbol hay CUATRO (`sovos-reachcore` 492, `finkok` 146, `sw-sapien` 122, `edicom` 102 líneas) más `pac-router.ts` (244, cableado a REST) y `simulacion.ts` — incluido el esqueleto de SW Sapien, el "recomendado". Afirmación imprecisa, no falsa. NUEVA.
7. **Dos módulos con superficie propia que ningún rector planifica ni recorta.** GraphQL entero (`src/api/graphql`, 918 líneas, segunda API paralela a REST) y el módulo blockchain multi-cadena (`src/services/blockchain`, 1 341 líneas: `chain-adapters.ts` con CHAIN_CONFIGS, `bitcoin-anchor.ts`, ruta REST propia en `src/index.ts:33`, cableado en `posting.ts:8`). Ambos aparecen en los rectores SOLO como dos de las 4 copias del SQL de saldos (E4.2); la atestación es E1.4, pero la superficie multi-chain/REST/GraphQL no tiene fila, criterio ni decisión de recorte. Parcialmente nombrada (vía E4.2); el gobierno de la superficie es NUEVA.

RECOMENDACIONES

1. **(S) Dejar de fijar cifras en §1 y §4 o regenerarlas del medidor.** Un pase hoy corrige las siete cifras de §1, las cabeceras F01/F03/F04, la cola (179) y el objetivo (1380); mejor aún: que el plan estampe su bloque de cifras con el mismo generador que ya regenera `docs/cli-command-catalog.md`, para que la violación de §7 sea imposible, no solo prohibida.
2. **(S) Arreglar `pac create` (fase vacía) y añadir la invariante al `--check`**: toda fila declara fase ∈ {1,2,3}. Dos líneas en `scripts/catalogo-estado.ts` y el trinquete existente la vigila para siempre.
3. **(M) Cumplir §7: el criterio "doctor sin huérfanos nuevos".** Congelar la lista actual de exports huérfanos (earlyPaymentDiscount, calculateBenefitsForPaycheck, los 4 adaptadores de integración) como línea base que sólo encoge — el patrón exacto de `LINEA_BASE` del auditor (`audit.ts:197`), que ya demostró funcionar.
4. **(S) Extender RECLAMADAS a módulos, empezando por los adaptadores**: sendgrid reclamado por F03 (es un dato que cambia la decisión de invoice send: cablear es más barato de lo que el plan cree), stripe/conekta/s3 con dueño nombrado o retirados como costing.
5. **(S) Corregir §5** enumerando los 4 adaptadores PAC y su estado real (cuál es esqueleto, cuál completo, que pac-router ya está cableado a REST) — la decisión de Víctor se toma mejor sabiendo que SW Sapien ya tiene arranque.
6. **(M) Decidir GraphQL y la superficie blockchain multi-cadena**: entran al plan con compuerta (E4.2 ya toca a ambos) o se congelan/retiran con criterio. Hoy son ~2 250 líneas de superficie sin fila, sin criterio y sin decisión de recorte — la definición exacta de lo que S0.5 vino a impedir.
7. **(S) Actualizar las "mayores" de F09–F12 con los datos de hoy**: report 10 encabeza la cola y además está compuesta por E4.2 — nombrarla refuerza el prerrequisito que el propio plan declara ("E4.2 completo como prerrequisito de report").