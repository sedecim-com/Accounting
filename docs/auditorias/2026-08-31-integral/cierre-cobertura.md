He terminado el censo, la clasificación y el contraste contra código. Entrego el informe.

---

**CENSO (base de la auditoría)**

15 paquetes, 147 tareas confirmadas por grep sobre plan-cierre.txt: E0.0:3 · E0.1:12 · E0.2:12 · E0.3:10 · E1.1:8 · E1.2:10 · E1.3:8 · E1.4:11 · E2.1:10 · E2.2:7 · E3.1:9 · E3.2:11 · E4.1:13 · E4.2:12 · E5.1:11 = 147. Decisiones: 16 secciones «Decisiones que alguien debe tomar» con 52 preguntas con encabezado propio (las ~85 del enunciado cuentan sub-decisiones inline). Estado que el propio documento declara (27-ago): 0 terminados, 12 parciales, 3 sin empezar (E1.3, E3.2, E5.1). Tablero vivo hoy (`npm run plan:status`, HEAD 5d24463): 9/15 en verde; abiertos E1.2 (2/3), E1.3 (0/1), E1.4 (1/2), E4.1 (2/3), E4.2 (1/2), E5.1 (5/6).

Clasificación por paquete (contrastada, no leída del documento):
- **HECHO**: E0.0, E0.1, E0.2, E0.3 (núcleo), E1.1, E2.2 (mitad catálogo único), E2.1 (sustancial). Muestras verificadas abajo.
- **ABSORBIDO por el Plan Maestro**: E1.2→F02 («cfdi_classifications: escribirla o retirarla»), E1.4 depreciación→F06/DEP-2 e inventarios→S0.4, E4.1 banca→F05 y nómina→F08 (motor presupuestado), E4.2 reportes→F09-F12 («E4.2 completo como prerrequisito de report») y worker→familias job/webhook del catálogo, E3.1→§5 (decisión PAC) + F02/F07/F08 + cola (pac/filing/diot/tax-mailbox), E5.1 puente→S0.3/rojo honesto del tablero, E0.3-i superficie de auditoría→8 filas `mnemosine audit`/`auditor` del catálogo (cli-command-catalog.md:2643-2674).
- **PENDIENTE nombrado**: los seis rojos del tablero (cada uno cita tabla/archivo exacto) más las dos decisiones de Victor (§5: PAC, e.firma).
- **CAÍDO**: ningún paquete entero, pero **8 fragmentos de tarea** que ni el Plan Maestro ni el catálogo comprometido ni los criterios cargan — son las brechas 2-6.

---

**FORTALEZAS**

1. **La absorción a nivel paquete fue real y auditada**. La sección «Plan unificado» del plan de cierre validó afirmación por afirmación en ambas direcciones (plan-cierre.txt:346-406) y el Plan Maestro disuelve los abiertos donde tocan: F02 carga E1.2/media E1.3, F06 carga DEP-2, F08 reescribió el criterio de E4.1 para medir salida y no siembra, F09-F12 carga E4.2 completo. Los seis rojos vivos del tablero citan el objeto exacto (src/plan/criterios.ts:1104-1126 nombra paycheck_taxes/employer_tax_liabilities/garnishments; :794 nombra cfdi_classifications).
2. **Lo marcado hecho resiste el contraste con código** — 6 de 6 muestras pasaron: cero consultas contra la tabla `entities` (grep en src, E0.2-e); `MISSING_ROLE_ACCOUNT` nombra el rol y el comando de siembra (src/services/accounting/ar-ap-posting.ts:69-75, E1.1-f); vitest.config.ts + .github/workflows/ci.yml existen (E0.1-a/E0.0-b); scripts/e2e-reversal.ts y e2e-arap.ts ya no existen (portados, E0.1-i/j); src/services/mexico/cfdi.ts borrado rescatando catálogos a sat-catalogs.ts (E1.4-i, confirmado por el propio catálogo:1908); la validación de asientos acota cuentas por entidad en el SQL (src/services/accounting/validation.ts:47-52 — cierra el quinto camino de ataque que TEN-1 dejó enumerado).
3. **Los criterios aprendieron de sus propios falsos verdes**: la cuarta redacción del criterio de rutas de E2.1 documenta por qué las tres anteriores se rompían solas (criterios.ts:915-984), y AUD-6 reabrió E1.2/E4.1 degradando el tablero de 12 a 9 — el instrumento acepta bajar.
4. **E5.1 está más construido de lo que el plan de cierre sabía** (lo marcó «sin empezar» el 27-ago): onFailover existe (src/cli/mnemosine.ts:200+, E5.1-d), el backstop determinista de UUIDs/RFCs/folios existe (src/ai/compaction.ts:259, mitad de E5.1-c), scripts/generate-cli-reference.ts (E5.1-j) y build-niif-indice.ts (E5.1-i) existen, skills/skill-drafts.ts (E5.1-g/h) existe.

---

**BRECHAS** (por gravedad)

1. **E3.2 es un falso verde vivo en el tablero, de la clase exacta que AUD-6 purgó** — NUEVA (ningún documento la nombra; AUD-6 nombró la clase, no esta instancia). El único criterio de E3.2 (criterios.ts:1066-1074) pasa si `/sat\s+download|descargaMasiva|SolicitaDescarga/i` aparece en src; lo único que matchea son **dos cadenas de prosa** de una pregunta de política (src/services/policy/pending-catalog.ts:392 y :398: «direct SAT download … is assumed»). No existe descarga masiva alguna: la familia `sat` del CLI solo tiene `cred add/status/audit/revoke` (src/cli/sat-commands.ts:66-272), no hay SOAP del SAT, ni lector ZIP, ni `sat download`/`sat verify` (E3.2-j). Agrava: el Plan Maestro §5 **redefine** E3.2 como «falta que un despacho cargue una credencial», cuando el paquete son 11 tareas de motor (~7.2 sem) — «Traer los CFDI del SAT y saber si siguen vivos», incluida la reversa de facturas contabilizadas que el emisor canceló (E3.2-i, riesgo fiscal directo). El tablero dice ✅ 1/1, el plan de cierre decía «sin empezar», y el código le da la razón al plan de cierre.
2. **E1.4-a cayó con la fuga de datos viva** — CAÍDA (el plan de cierre la lista en «Se puede empezar hoy»; ni Plan Maestro, ni catálogo, ni criterios la cargan). El range proof sigue guardando el valor en claro y el blinding factor dentro del blob persistido: src/services/blockchain/crypto-service.ts:124-126 (`_test_value`, `_test_bf`, bajo el comentario «DO NOT store the value in a real proof») y el orquestador lo escribe a `blockchain_attestations.range_proof` (src/services/blockchain/orchestrator.ts:140-147). Tampoco existe la migración de limpieza de filas ya escritas que la tarea exigía (031-039 revisadas: ninguna). Mitigado hoy porque /public/v1 está apagado por omisión, pero el compromiso que «no revela el importe» lo revela.
3. **E2.2-e (segregación de funciones) cayó del compromiso sin decisión escrita** — CAÍDA parcial: `checkSoDViolations` está exportada con **cero llamadores** (grep en src fuera de auth.ts: vacío; el propio catálogo lo confiesa en cli-command-catalog.md:1112: «sin un solo llamador: las reglas existen y no se aplican en ningún punto»). Las filas que la cablearían (`ap sod show`, `sod matrix show/set`) son urgencia 2-3 — fase de respaldo, fuera de las 380 comprometidas. E2.2 luce ✅ 2/2 porque sus criterios miden el catálogo único y el secreto de producción, no la SoD. El diferimiento puede ser correcto, pero hoy es tácito.
4. **E2.1-e a medias: el arranque no es fail-closed ante RLS inerte** — CAÍDA parcial (el plan de cierre pedía «configuración, siembra y arranque fail-closed»; ningún documento vigente lo carga). `advertirSiElRolIgnoraRls` solo emite `logger.warn` cuando el rol conectado tiene BYPASSRLS/superusuario (src/index.ts:52-75) — también en producción, donde «un error que olvide filtrar por inquilino devuelve las filas de todos». Los 3 criterios verdes de E2.1 miden middleware y rutas, no el rol de conexión.
5. **Tres huecos de E5.1 confesados en comentarios y sin dueño** — CAÍDOS (ni Plan Maestro, ni criterios, ni filas del catálogo; no son comandos, así que el catálogo no puede cargarlos): (a) E5.1-c mitad importes — src/ai/compaction.ts:18-19: «monetary amounts are protected by instruction ONLY — there is no deterministic amount backstop», en un contable donde el importe es la carga útil; (b) E5.1-b rehidratación real — la propia opción lo confiesa: `--continue` = «transcript continuity; the model context starts fresh» (src/cli/mnemosine.ts:605); (c) E5.1-f — src/ai/providers/prices.ts sin fecha de corte ni aviso de obsolescencia (grep cutoff/corte/vigencia: vacío).
6. **Cabos menores caídos**: E2.2-g (sección generada de identity-access.md con test de sincronía — el archivo no existe en docs/); E4.1-i (subsidio al empleo: entregar el remanente — F08 nombra escritores, beneficios y transmisiones, no esto); las claves `efirma_max_accesos_diarios`/`efirma_accion_anomalia`/`pac_ofrece_descarga` de E1.3 sobreviven solo en el texto del criterio — F02 solo reclama los lectores «de ingesta», ningún flujo es dueño de las otras tres.
7. **Meta-brecha: la herencia se hizo a nivel prosa, no a nivel tarea** — NUEVA. Las 147 tareas no tienen disposición individual registrada (hecho / absorbido-dónde / retirado-por-decisión); las brechas 2-6 son exactamente lo que se cuela por esa malla. El plan de cierre queda como «registro de lo ejecutado» (pie del Plan Maestro) pero es la única fuente de ~10 obligaciones que ya nadie relee.

---

**RECOMENDACIONES**

1. **(S)** Poner E3.2 en rojo honesto hoy: reescribir su criterio para exigir código real (p. ej. un servicio/comando de descarga con transporte, no una regex que come prosa) o degradarlo al patrón del puente de E5.1 (rojo con enunciado de aspiración). En el mismo commit, corregir §5 del Plan Maestro: E3.2 = motor de descarga/verificación (11 tareas) + credencial, no solo la credencial.
2. **(S)** Ejecutar E1.4-a: quitar `_test_value`/`_test_bf` del proof persistido (o dejar de persistir el blob), migración 040 que purgue `range_proof` de las filas escritas, y criterio que falle si el blob persistido contiene el valor del asiento.
3. **(S decidir / M cablear)** Resolver E2.2-e por escrito: o se difiere la SoD a fase 2 con una línea en el Plan Maestro que lo diga, o se cablean los dos puntos de invocación que la tarea nombraba (preventiva al asignar rol, maker-checker al aprobar) con criterio «checkSoDViolations tiene ≥1 llamador».
4. **(S)** Arranque fail-closed: con NODE_ENV=production y rol con BYPASSRLS, negarse a arrancar salvo bandera explícita de anulación; el warn se queda para desarrollo. Criterio en E2.1.
5. **(S cada uno)** Registrar los tres huecos confesados de E5.1 como criterios en rojo honesto (importes sin backstop, `--continue` sin rehidratar, precios sin fecha de corte) — el texto ya está escrito en los propios comentarios; hoy son deuda que solo el código conoce.
6. **(M)** Un pase único de disposición por tarea: tabla de 147 renglones (ID → hecho/absorbido-en/retirado/caído) generada una vez y guardada como registro, con las ~52 decisiones marcadas resuelta/heredada/caducada. Es la red que evita el próximo E1.4-a; este informe ya deja hecho el primer borrador.

Archivos citados: /Users/victor/projects/Accounting/src/plan/criterios.ts · src/services/policy/pending-catalog.ts · src/cli/sat-commands.ts · src/services/blockchain/crypto-service.ts · src/services/blockchain/orchestrator.ts · src/api/rest/middleware/auth.ts · src/index.ts · src/ai/compaction.ts · src/ai/providers/prices.ts · src/cli/mnemosine.ts · src/services/accounting/validation.ts · src/services/accounting/ar-ap-posting.ts · docs/cli-command-catalog.md