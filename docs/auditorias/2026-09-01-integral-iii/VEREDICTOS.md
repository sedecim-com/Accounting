# Los once titulares, sometidos a escépticos

Cada titular de la auditoría III pasó por un agente con el encargo explícito de refutarlo.

## Los titulares de la auditoria II, re-verificados — **SOSTIENE-CON-MATIZ**

**Razón.** La conclusión resiste entera: abrí las nueve y las nueve siguen vivas en HEAD (36ca62e), no en cfe40c6. FLUJOS_CERRADOS sigue siendo `{ // 'F01': … }` (src/plan/criterios.ts:243) y su única comprobación viva sigue vigilando la auditoría de agosto (:246); mexico-cfdi.md:4 sigue diciendo «credited against BANKS» contra cfdi-taxonomy.ts:166-167, sigue teniendo un solo commit en su historia (4eeee63) y tests/ai/docs-mexico-cfdi.spec.ts no existe; CORRECTIVO_RE sigue en scripts/costo-por-fila.ts:68; ledger-checks.ts y doctor-service.ts no mencionan `ending_balance` ni una vez; pac-router.ts:20-22 registra Finkok/SW/Edicom y no Sovos, que sí está en PAC_ADAPTERS; ingest-thresholds.ts:60-62 sigue dejando que el archivo encienda el interruptor sin comparar contra la política, y policy-service.ts consulta la sombra por entidad pero hace UPDATE `WHERE tenant_id = $4 AND key = $5`; migrate.ts:83-90 sigue siendo BEGIN→query(sql)→INSERT→COMMIT sin un solo set_config, y sólo 025 y 026 lo ponen ellas mismas —037, 040 y 043 no—; la línea de --exigir es literalmente la misma; y `pg_dump` sólo aparece en los informes de auditoría. Lo que CAE es el argumento con que el titular sostiene esa conclusión. Dice «desde 689458a el árbol sólo se movió 101 líneas en 9 archivos… lo único que cambió es que los tres documentos rectores ahora los describen». En el árbol donde se publica esto, `git diff --shortstat 689458a HEAD -- src scripts tests` da 113 archivos y +749/-287 (135 archivos y +4553 contando todo), y lo movido no es documental: el PR 7 metió ESLint 9 con información de tipos y un job `lint` nuevo en .github/workflows/ci.yml:46-59 —una puerta de calidad real, no un párrafo—, y migrate.ts ganó un `.catch` que impide que una migración fallida salga en verde. El titular mide un árbol de hace diez commits y lo presenta como el de hoy.

**Matiz.** Sustituir la causa por la que ahora es cierta, que además es más fuerte: «Las nueve titulares de la auditoría II siguen vivas en el árbol de hoy (36ca62e): cero cerradas. Y no por quietud —desde 689458a entraron 113 archivos y 749 líneas en src/, scripts/ y tests/, incluida una capa de ESLint 9 con información de tipos y su propio job de CI (.github/workflows/ci.yml:46-59)—, sino porque ninguna de esas líneas toca uno solo de los nueve puntos: los seis archivos señalados (criterios.ts, ingest-thresholds.ts, ledger-checks.ts, pac-router.ts, mexico-cfdi.md, costo-por-fila.ts) son byte a byte los de 689458a, y los dos que sí cambiaron lo hicieron lejos del hallazgo —ci.yml ganó el job de lint, no tocó la lista de --exigir; migrate.ts ganó un .catch en :139-150, no el contexto de inquilino de :83-90.» Corregir además la única cita que derivó: la línea literal de --exigir es hoy `.github/workflows/ci.yml:109`, no `:94` (el job de lint la empujó quince renglones); el contenido de la lista es idéntico. Y bajar el «lo único que cambió es documental» a «lo único que absorbió los nueve es documental»: es lo que el lente prueba y no admite la lectura de que el código estuvo parado.

**Consecuencia.** El despacho planea F03 creyendo que el registro de brechas se ejecuta, cuando lo que se movió fue otro carril. Tres cosas se pagan a la vez el día del incidente: la 040 —la purga del secreto que el compromiso revelaba— se registró como aplicada habiendo afectado 0 filas si se corrió como mnemosine_owner (NOBYPASSRLS bajo FORCE RLS y sin app.current_tenant), y nadie compara rowCount, así que el repositorio cree hecha una purga de seguridad que no está hecha; no hay pg_dump en ningún lado con el que comparar ni restaurar; y el agente sigue leyendo su propio mexico-cfdi.md, acredita el CFDI PUE contra BANCOS y duplica la salida cuando entra el movimiento bancario por conciliación, que es la falla que cfdi-taxonomy.ts describe palabra por palabra. Sumado: el presupuesto de F03 se arma sobre el 0,7 % de cola correctiva que costo-por-fila.ts sigue imprimiendo y la medición honesta pone entre 11,8 % y 51,7 %, así que el tramo se queda sin fondo a la mitad y encima con la deuda de las nueve encima.

---

## Escala, rendimiento y el dia que haya datos — **SOSTIENE-CON-MATIZ**

**Razón.** Levanté un clúster PostgreSQL 15.17 propio con el DDL literal de src/database/migrations/001_core_schema.sql:222-298, la función de src/database/migrations/014_rls_tenant_isolation.sql:26 y los bloques DO $mig$/$grants$/$child$ de src/database/rls-policies.sql aplicados verbatim, y cargué 792 000 líneas / 198 000 asientos / 10 000 cuentas / 50 entidades. Las tres refutaciones que el propio lente propone FALLAN: (a) el plan bajo mnemosine_app reproduce literalmente el SubPlan 3 sobre journal_entries con `loops=400000` y `Index Scan using idx_jel_account (rows=2000 loops=200)` — el planificador NO aplana el EXISTS; (b) la razón es 10,4× en reloj de pared sin instrumentación de EXPLAIN (mediana 840 ms como mnemosine_app contra 81 ms como superusuario), muy por encima del umbral de 3×; (c) añadir tenant_id a journal_entry_lines y sustituir la política por `USING (tenant_id = app_current_tenant())` baja de 840 ms a 193 ms, más de la mitad. Las dos citas del titular son exactas: rls-policies.sql:176-180 es el CREATE POLICY con el EXISTS, y report-service.ts:300-316 es el SQL de la balanza. Ninguna de las dos cambió desde cfe40c6 (el PR 7 de ESLint sólo tocó report-service.ts:196, renombrando `expr` a `_expr`), así que el árbol de hoy no cerró nada. Y el camino es el de producción: src/database/rls-guard.ts impide arrancar en production con un rol que ignore RLS, y src/index.ts:185 pone toda la API bajo tenantContext.

**Matiz.** Dos correcciones de enunciado, ambas de magnitud, no de mecanismo. PRIMERA: el «17×» no es una constante. En mi corrida independiente la razón es 10,4× (840 ms contra 81 ms), y el arreglo da 4,3×, no 7×. El 17× del lente sale de comparar contra una línea base de superusuario que su máquina resolvió en 46 ms —casi con seguridad con unión hash PARALELA— mientras que la mía eligió hash no paralela. Parte del «17×» mide la pérdida de paralelismo, no el SubPlan por línea. El enunciado honesto es «un orden de magnitud», entre 10× y 17× según lo que el planificador consiga en la ruta sin RLS. SEGUNDA, y más importante: «sobre 792 000 líneas» atribuye el costo al tamaño de la base, y no es eso. El costo lo fija el número de líneas HISTÓRICAS de la entidad consultada, y —esto es lo que el plan prueba y el titular no dice— se paga ENTERO aunque se pida un solo mes: para devolver los 33 336 renglones de marzo, la cualificación de seguridad recorre y sondea las 400 000 líneas de toda la historia de la entidad ANTES del filtro de fecha. Medí la misma balanza sobre una entidad chica (8 000 líneas): 14,7 ms contra 7,4 ms, razón 2× y cero problema operativo. El enunciado correcto es: «la política encarece la balanza en proporción a la historia acumulada de la entidad consultada —irrelevante bajo ~50 000 líneas, un orden de magnitud a partir de ~400 000— porque la barrera de seguridad se evalúa sobre todas las líneas del ejercicio y de todos los anteriores, no sobre las del periodo pedido».

**Consecuencia.** El escenario que sí se sostiene no es el del titular (50 despachos concurrentes) sino uno más lento y más seguro: el trinquete. Como la barrera se evalúa sobre TODAS las líneas históricas de la entidad, la balanza mensual de un mismo cliente se encarece linealmente cada ejercicio aunque el mes pedido no crezca. Un cliente de 20 000 asientos al año llega a 400 000 líneas en el quinto ejercicio y su balanza de un mes pasa de ~40 ms a ~840 ms sin que nada haya cambiado en el producto ni en el uso. Ahí se compone con el hallazgo 3 del mismo lente, que verifiqué en el código: src/database/connection.ts:54-58 construye el Pool sólo con connectionString/min/max, no hay statement_timeout ni connectionTimeoutMillis, y DATABASE_POOL_MAX vale 20 (src/config/index.ts:53). Con RLS forzada cada consulta bajo contexto de inquilino toma una conexión durante toda su transacción (connection.ts:159-172), así que veintiún cierres de mes simultáneos —el día más predecible del calendario de un despacho— agotan el pool y la petición 21 espera sin tope ni error. El costo al despacho no es una factura de CPU: es que el cierre de mes, el único día en que todos los contadores piden la balanza a la vez, es exactamente el día en que la aplicación se cuelga sin mensaje. Y hay una trampa en la salida: el arreglo probado (columna tenant_id + política directa) choca de frente con el disparador de inmutabilidad de 041_el_mayor_inviolable.sql:68-99, que rechaza cualquier UPDATE sobre líneas de asientos posteados — el relleno de la columna no tiene camino de migración sin tocar antes ese disparador.

---

## Que prueban de verdad las 170 suites — **SOSTIENE-CON-MATIZ**

**Razón.** Hice la comprobación que el propio lente nombra y el titular resistió: invertí el signo en src/services/reporting/report-service.ts:307 y las 10 specs que tocan el módulo (tests/services/reporting/*, tests/cli/report-command.spec.ts, tests/ai/tools/*, 149 pruebas, incluidas las 111 aserciones de report-service.spec.ts) quedaron verdes; los dos rojos del run completo venían de mutantes «GUTTED» de otro refutador en iva-ppd-reclass.ts / ledger-checks.ts / period-close.ts, no de mi mutación. Las citas son exactas: :3 mockea `query`, :43 arma el fixture como `ending_balance: String(Number(debit) - Number(credit))` — el test recomputa lo que el SQL hace — y `grep -rn "reporting/report-service" tests/integration` no devuelve nada, ni siquiera tras los 10 commits que main ganó desde cfe40c6. Pero el enunciado falla en un absoluto medible: queryTrialBalanceRows SÍ se ejecuta contra Postgres, transitivamente, desde src/services/reporting/materialized-view-service.ts:108 dentro de getReportingViewStatus, que tests/integration/serie-y-refresco-r3.int.spec.ts:88-108 llama tres veces y cuyos totales compara numéricamente contra mv_trial_balance.

**Matiz.** No es que «la balanza nunca se ejecute contra Postgres»: su SQL sí corre ahí (materialized-view-service.ts:108 ← serie-y-refresco-r3.int.spec.ts:88-108) y sus columnas debit_total/credit_total quedan indirectamente vigiladas por el detector de deriva. Lo que no tiene una sola prueba contra Postgres es la columna `ending_balance` —la que el contador lee— y los otros CINCO reportes completos (balance sheet, income statement, general ledger, aged receivables, aged payables; son seis funciones, no «cuatro más la balanza»). Enunciado correcto: «Ninguna prueba evalúa jamás el saldo que el SQL calcula: invertir el signo en report-service.ts:307 sobrevive las dos suites, porque la única prueba del módulo mockea `query` y recomputa la resta en su fixture, y ningún archivo de tests/integration importa report-service». Además la cifra «2 440 pruebas» está caduca en el árbol de hoy: la unitaria reporta 143 archivos / 2 205 pruebas y la de integración 29 archivos.

**Consecuencia.** Cualquiera que toque esa línea al añadir un filtro de periodo invierte el signo de TODA la balanza y CI queda verde: `is_balanced` se calcula desde debit_total/credit_total (report-service.ts:341), así que el reporte imprime «balanced» mientras el activo sale en negativo y el pasivo en positivo, tanto en el CLI (report-command.ts:266 consume `ending_balance` en crudo) como en la herramienta del agente (report-tools.ts:69). El despacho firma una balanza con todos los signos volteados y sello de cuadrada; el error se descubre cuando un tercero la cruza contra el SAT — y la reputación que se pierde ahí es la del despacho, no la del repositorio.

---

## La aritmetica del cierre y del ejercicio — **SOSTIENE**

**Razón.** Verifiqué las dos citas abriendo los archivos y ambas dicen exactamente lo que el informe afirma: report-service.ts:588 filtra sólo `je.status = 'posted' AND je.entry_date BETWEEN $2 AND $3` (cero apariciones de entry_type en queryIncomeStatementRows, 568-596), y period-close.ts:312 llama a generateClosingEntries con `new Date(period.end_date)`, que en :532-540 crea el asiento `'closing'` con autoPost dentro del rango que el reporte consulta. Corrí la comprobación dinámica que el propio lente pide, contra Postgres real con base efímera: ventas 100.000 (4100) y sueldos 50.000 (5200), hard-close de diciembre, y getIncomeStatement por la misma ruta del CLI dio `ANTES net_income = 50000.0000` / `DESPUES (2026-01-01..2026-12-31) net_income = 0.0000, revenue = 0.0000, filas = 0` / `DICIEMBRE SOLO net_income = -50000.0000, revenue = -100000.0000`, con dos asientos `entry_type='closing'` fechados 2026-12-31. Además no lo arregló el árbol de hoy: `git diff cfe40c6..HEAD` sobre esos dos archivos son 3 líneas puramente cosméticas de ESLint (FiscalPeriodStatus.SOFT_CLOSE y `_expr`).

**Matiz.** Dos precisiones que afinan el enunciado sin tumbarlo. (1) El estado anual no imprime líneas «en ceros»: imprime CERO líneas. El `HAVING COALESCE(SUM(debit - credit),0) != 0` de report-service.ts:576 elimina toda cuenta que el cierre neteó, así que el estado sale vacío con «Net income 0.0000» — peor que ceros, porque parece un ejercicio sin operaciones en vez de un cálculo mal filtrado. (2) El daño es de fin de ejercicio, no de cualquier cierre: generateClosingEntries sólo dispara si `isYearEnd` (period-close.ts:310), así que los cierres duros de enero a noviembre no contaminan nada; lo que se corrompe es el ejercicio completo y cualquier rango que incluya el 31-dic. En cambio el alcance por superficie es MÁS ancho de lo que el titular sugiere: la misma consulta sin filtrar la consumen el CLI (report-command.ts:391), el REST (api/rest/routes/reports.ts:129) y la herramienta del agente (ai/tools/report-tools.ts:138) — las tres mienten igual.

**Consecuencia.** El estado de resultados anual de un ejercicio cerrado es justo el documento que el despacho entrega: el que acompaña la declaración anual, el que ve el auditor externo, el que se anexa a un crédito bancario. Después del hard-close de diciembre ese documento sale vacío con utilidad 0,00 y sin ninguna advertencia — ni un warning, ni una nota al pie, ni una fila. El contador que lo imprima para firmar el ejercicio no verá un error, verá un ejercicio que aparentemente no operó, y el modo de fallo silencioso es exactamente el que se firma sin revisar. Peor todavía, el reporte de diciembre solo devuelve ingresos NEGATIVOS por el importe anual (-100.000 en mi corrida), cifra que en una comparativa mensual o en un anexo al dictamen es una declaración falsa con cara de dato. Y como las tres superficies (terminal, API REST y el agente de IA que redacta explicaciones) comparten la consulta rota, no hay una segunda fuente donde el error se note por discrepancia: todas coinciden en la misma cifra equivocada, que es la peor forma de coincidir.

---

## Control interno: lo que un auditor externo exigiria — **SOSTIENE**

**Razón.** Hice la comprobación que el propio lente ofreció como verdugo y salió a su favor: `grep -rn "requirePermission|hasPermission|permissionsOf" src/cli/ src/ai/` devuelve CERO (los tres símbolos sólo viven en src/api/rest/ y en su definición src/auth/roles.ts:141/155/160, más tests/auth/roles.spec.ts), y `resolveReviewer` (src/ai/draft-service.ts:289-301) resuelve `SELECT id, email FROM users WHERE tenant_id=$1 AND is_active=true AND email=$2` — sin contraseña, sin token, sin rol, sin `accessible_entities`; el único bcrypt del árbol es un `hash` de alta en src/cli/init/s2-users.ts:114, no hay ningún `compare` ni comando `login`. La cadena del ataque está entera y sin bifurcaciones: flags.ts:76 declara literalmente `-u, --user <email>` como «acting user, for attribution and permissions» (idéntico en el árbol auditado 61379d0 y en HEAD 36ca62e), `withContext(post)` en src/cli/entry-command.ts:765 la aplica, la acción lee `resolveReviewer(ctx.tenantId, opts.user)` en :805 y pasa ese `userId` a `postJournalEntry`, que compara `!entry.source_type && entry.created_by === userId` en src/services/accounting/posting.ts:317-333 antes de escribir `posted_by = $2` (:345-350) y la fila de auditoría con ese mismo `userId` (:352-361). Las citas del informe se abrieron una a una y dicen lo que afirma; los 10 commits que main ganó tras cfe40c6 (PR 7 de ESLint) tocaron entry-command.ts y posting.ts sólo para lint y no cerraron nada: la comprobación sigue dando cero hoy.

**Matiz.** Dos precisiones, ninguna toca la tesis. (1) Cita: en 61379d0 `withContext(post)` es :764 (correcto) pero la lectura de `opts.user` es :804, no :803 (en HEAD, :765 y :805). (2) El titular se enuncia sobre `entry post`, y el defecto es del contrato de la bandera: hay 45 lecturas de `opts.user` en diez familias del CLI —`bill approve`, `vendor` (tres, incluidas las bancarias), `period`, `invoice`, `customer`, `sat`, `memory`— todas por el mismo `resolveReviewer` sin credencial. Y conviene desarmar de antemano el «¿y qué, si el operador ya tiene el DATABASE_URL?»: no se afirma escalada de privilegio de base —`mnemosine_app` tiene DML sobre todo (scripts/provision-roles.sql:73)— sino falsificación de evidencia. Por SQL crudo el impostor tendría que mover él mismo `account_balances` y fabricar la fila de bitácora, y si no la fabrica, `ledger check --check audit-trail` (src/services/accounting/ledger-checks.ts:87-105) lo caza como bloqueante; por `--user` el sistema se la escribe solo, dentro de la transacción y en una tabla append-only por disparador (033), y ese mismo control la certifica limpia porque sólo comprueba que la fila exista, no de quién es.

**Consecuencia.** Un despacho de dos o más manos enciende `segregacion_de_funciones = 'exigir'` porque el panel le promete que «entry post rejects the drafter posting their own entry» (src/services/policy/pending-catalog.ts:439-452) y porque el tablero E0.1 lo pinta verde (src/plan/criterios.ts:655-690). Ana redacta una póliza manual de 800 000 MXN y la postea ella misma tecleando `--user beto@despacho.mx`: el asiento entra al mayor, `journal_entries.posted_by` y `audit_log.user_id` dicen Beto, el mayor queda inmutable por la 041 y la bitácora por la 033. El costo tiene dos caras. Frente al tercero: el auditor externo que pruebe el control de cuatro ojos por la superficie primaria descubre que la columna de responsabilidad de TODA la terminal es una declaración del operador, no una prueba — con lo que ninguna conclusión de responsabilidad se apoya en `audit_log`, y de paso el hallazgo contamina las 45 llamadas (aprobación de facturas, alta y aprobación de datos bancarios de proveedor, cierre y reapertura de periodo). Frente al que sufre la falsificación: Beto queda nombrado por escrito, en un registro que el propio sistema publicita como inviolable, como autor de un acto que nunca vio, y el producto no ofrece un solo dato para desmentirlo (`audit_log.ip_address`, `user_agent`, `request_id` y `approver_id` los deja nulos toda escritura de la terminal, src/services/audit/audit-log.ts:71-74). Vender un control de cuatro ojos que una bandera documentada derrota es exponerse a que un cliente lo descubra el día en que ya hubo fraude y la bitácora acuse al inocente.

---

## NIF e IFRS, mas alla del fisco — **SOSTIENE**

**Razón.** Verifiqué las cuatro citas y todas dicen lo que el informe afirma: entity-accounting.ts:67 corre seedAccountRoles antes de seedPayrollAccountMapping en :77; account-roles-seed.ts:126 crea 5200 como «Devoluciones y Descuentos sobre Compras · credit · cogs»; payroll-account-mapping-seed.ts:43 define su propio 5200 «Sueldos y Salarios» pero :207 (`if (byCode.has(spec.code)) continue`) lo descarta al encontrar el ajeno, y :77 mapea wages_expense a ese id. chart-seed.ts no contiene ninguno de los cinco códigos en disputa, así que no arbitra. Ejecuté la comprobación que el lente pidió: sin Postgres, corrí los dos sembradores reales en el orden real contra un cliente pg falso precargado sólo con 1111 y 2130, y salió literalmente wages_expense→5200 Devoluciones y Descuentos sobre Compras credit cogs, imss_payable→2150 Anticipos de Clientes, infonavit_payable→2160 Sueldos por Pagar, garnishment_payable→2170 IMSS por Pagar, benefits_payable→2180 IEPS por Pagar — los cinco buckets que el lente nombró. No es código muerto (POST de nómina en src/api/rest/routes/payroll.ts:234 → gl-posting-service.ts:102 carga el bruto ahí) ni está arreglado: git show 8334ced sale vacío sobre payroll-account-mapping-seed.ts y cambia una sola línea ajena en account-roles-seed.ts.

**Matiz.** Dos correcciones, ambas AMPLÍAN el hallazgo, no lo recortan. (1) «toda nómina mexicana» se queda corto: seedAccountRoles corre para cualquier país, así que una entidad USA también recibe wages_expense→5200 «Devoluciones y Descuentos sobre Compras», fit_payable→2150 «Anticipos de Clientes», garnishment_payable→2170 «IMSS por Pagar» y benefits_payable→2180 «IEPS por Pagar». (2) El daño más grave no es la línea de sueldos —que al menos llega a resultados, aunque mal clasificada por función (NIF B-3)— sino los cuatro choques de PASIVO, porque las cuentas víctima ya tienen dueño en otro subsistema: 2150 es el rol `anticipo_clientes` del motor de facturación y 2180 es `ieps_por_pagar`. Es decir, las cuotas IMSS se acumulan dentro de los anticipos de clientes y las deducciones de prestaciones dentro del IEPS por enterar: contaminación cruzada entre nómina, CxC y una cuenta de impuesto que se declara. Única excepción real, angosta: una entidad onboardeada cuyo propio catálogo ya traiga un 5200 correcto.

**Consecuencia.** La primera nómina que el despacho timbre y contabilice para cualquier cliente deja el balance CUADRADO y los estados MENTIROSOS, que es el peor de los dos modos de fallo porque nada truena. Escenario concreto: cliente MX, nómina quincenal de 800 000 MXN de bruto con 60 000 de cuota IMSS obrero-patronal. El asiento cuadra, así que el cierre de periodo cierra y ningún control lo detiene: doctor-service.ts:240 y plan/criterios.ts:1644 sólo comprueban que payroll_account_mapping TENGA filas, no que apunten a la cuenta correcta, y tests/services/payroll/payroll-account-mapping-seed.spec.ts corre el sembrador de nómina aislado —con un pg falso que nunca vio pasar al de roles— así que sus 13 pruebas pasan en verde sobre el defecto. Resultado: el estado de resultados reporta 800 000 de bruto como contra-costo dentro de COGS, con utilidad bruta y gastos de operación deformados en esa cifra cada quincena; el balance reporta 60 000 de IMSS como «Anticipos de Clientes», o sea pasivo por ingresos diferidos que el auditor circularizará contra clientes que jamás anticiparon nada; y las prestaciones se apilan en «IEPS por Pagar», una cuenta que alimenta una declaración al SAT. El despacho firma estados financieros con clasificación falsa, la conciliación IMSS-SUA/IDSE no ata contra ninguna cuenta, y el descubrimiento típico llega en la auditoría anual o en una revisión del SAT, cuando ya hay 24 quincenas contabilizadas así en toda la cartera y la reexpresión toca a cada cliente.

---

## Ingenieria de contexto: que lee y que sabe el agente — **SOSTIENE-CON-MATIZ**

**Razón.** El mecanismo es real y sigue vivo en el árbol de hoy (36ca62e; `git log cfe40c6..HEAD` sobre los cuatro archivos citados devuelve vacío). Verifiqué cada cita: `src/ai/tools/index.ts:26` fija `MAX_TOOL_RESULT_CHARS = 32000`; `index.ts:79-90` aplica `.map(withResultCap)` a TODAS las herramientas, `read_docs` incluida; `src/ai/tools/docs-tools.ts:51-53` (`readDoc` = `fs.readFileSync` entero) y `:70-72` (el esquema sólo acepta `topic`, sin offset ni sección). Reproduje el corte de forma determinista —`head -c 32000` sobre `src/ai/docs/cli-reference.md` (163 589 chars)— y calculé el marcador: 109 chars, total entregado 32 109, exactamente lo que midió el lente; el corte cae en la línea 832 y las secciones propias de las catorce familias empiezan en la 851 (`entity`) y siguen hasta la 3779 (`webhooks`), o sea las 14 caen fuera, y `grep -rc "mnemosine entry"` sobre todo `src/ai/docs/` da 28 aciertos y todos en `cli-reference.md` — ninguna otra ruta documental existe, mientras `system-prompt.ts:32` enruta «how to run a command / which flags exist → cli-reference» y `cli-reference.md:5-7` ordena «never invent a flag that is not listed here». La comprobación refutatoria que el propio lente propuso NO se dispara: la cadena no contiene `mnemosine entry post`, no excede 32 109 y sí termina en el marcador. PERO el enunciado miente en el reparto: dentro del corte sobrevive el bloque `## mnemosine (root)` (`cli-reference.md:60-130`), que nombra las CATORCE familias con su alias español y su resumen de verbos —`entry|poliza  Journal entries: draft, inspect, validate, post, reverse and void`, `invoice|factura`, `payment|pago`, `report|reporte`, `account|cuenta`…—. Lo que queda del otro lado no son las familias, son sus SECCIONES: los comandos hoja y sus banderas.

**Matiz.** Cambiar «trece de las catorce familias contables quedan del otro lado del corte» por: «las catorce familias sobreviven al corte sólo como una línea de la ayuda raíz (nombre, alias y verbos); lo que queda del otro lado son sus catorce secciones enteras —los comandos hoja y todas sus banderas—, 139 de los 181 encabezados del documento». Con eso cae también el escenario de fallo tal como está escrito: el agente NO puede responder «el sistema no ofrece un comando para contabilizar pólizas», porque la ayuda raíz le dice que `mnemosine entry|poliza` existe y que postea. El fallo real, que es el que hay que enunciar, es que el agente ve el nombre y no puede citar ni una sola invocación exacta: obligado a no inventar banderas, o se calla el comando o lo inventa contra su propia regla. Corregir además dos datos menores del informe: `ifrs-registry.json` no es tema de `DOC_TOPICS` ni lo puede entregar `read_docs` (`readDoc` concatena `.md`), así que su «61 % perdido» no es consecuencia del tope; y falta `niif-grupos.md` (32 295 chars) en la lista de documentos que exceden el tope, que son seis y no cinco.

**Consecuencia.** Un contador pregunta en chat cómo contabilizar una póliza o emitir una factura. El agente cumple el protocolo, llama `read_docs('cli-reference')` y recibe 32 109 caracteres donde ve que `entry|poliza` y `invoice|factura` existen pero no hay una sola invocación ni bandera. Ahí se bifurca y las dos ramas cuestan: o cumple `cli-reference.md:5-7` y se niega a dar el comando —el despacho pagó por un asistente que sabe que el comando existe y no sabe dictarlo, y el contador acaba en `--help` a mano—, o inventa `mnemosine entry post --fecha …` con la forma correcta y la bandera equivocada, y el contador ejecuta a ciegas contra el mayor. El agravante operativo es que el marcador de truncado le ordena al modelo «refine your query (filters, date ranges, pagination)» y el esquema de `read_docs` no admite ninguna de las tres: el modelo recibe una instrucción que no puede obedecer, así que reintenta la misma llamada y quema contexto y dinero en un bucle que nunca entrega nada nuevo. Y nada lo ve: `tests/ai/cli-reference.spec.ts:26-29` lee el archivo con `fs.readFileSync`, no la herramienta, así que el spec seguirá verde mientras el 80 % del documento no llega al modelo — es la misma clase de hueco que ese spec se escribió para cerrar, reabierto un piso más abajo.

---

## Operacion: observabilidad, fallos y el dia malo — **SOSTIENE-CON-MATIZ**

**Razón.** Las dos comprobaciones que el propio lente ofrecía para tumbarlo fallan: `grep -rn "runReaderTurn" src/` da exactamente tres resultados en `src/api/rest/routes/ai-webhooks.ts` (:42, :110, :118) más las declaraciones de `reader-agent.ts` (:29, :193, :234) — cero productores, y `src/index.ts:169` monta `aiWebhooksRouter`, que es `createAiWebhooksRouter()` sin deps (`ai-webhooks.ts:143`); no hay SELECT de reproceso sobre `ai_webhook_deliveries` fuera de `intake.ts`, y `webhooks` sólo tiene create|list|disable|deliveries (`src/cli/webhooks-command.ts:56,91,129,153`). El remate también resiste: `recordDelivery` inserta con `ON CONFLICT (token_id, document_key) DO NOTHING` (`intake.ts:238-241`) y la ruta retorna `duplicate` en `:105-108` ANTES de `processDelivery`, así que la reanudación que prometen `reader-agent.ts:240-241` y `ai-webhooks.ts:120-122` es inalcanzable incluso si algún día se cablea el lector. Pero el enunciado tiene tres imprecisiones que hay que corregir, y una de ellas se queda corta, no larga.

**Matiz.** Tres correcciones. (1) La cita es `src/index.ts:169`, no `:168` — 168 es la última línea del comentario. (2) El titular subestima el daño y por tanto el remedio: la tabla NO guarda el cuerpo — `028_ai_webhooks.sql:68-87` tiene id, token_id, tenant_id, entity_id, document_key, received_at, status, suspicion y drafts_created, y ninguna migración posterior la toca (`grep -rln ai_webhook_deliveries src/database/migrations/` devuelve sólo 028); `DELIVERY_COLUMNS` (`intake.ts:65-66`) lo confirma. Como `processDelivery` exige `rawBody` (`reader-agent.ts:192, 213-214`), una entrega almacenada no es «procesable más tarde»: es IRRECUPERABLE en el instante en que termina la petición HTTP. Cablear `runReaderTurn` no bastaría; hace falta además persistir el cuerpo. (3) No es un hallazgo nuevo: el propio árbol ya lo documenta — `docs/cli-command-catalog.md:2912` dice literalmente que el router se exporta «sin inyectar runReaderTurn» y que «en producción los webhooks de entrada solo registren entregas y nunca despierten al agente», `:2922` que «nada reprocesa una entrega almacenada», y `docs/plan-cierre-brechas.md:6881` ya razona la falta del cuerpo, con la reparación planeada como el tipo de tarea `webhook_reader_drain` (`:6951`). El enunciado correcto es «conocido, escrito y sin arreglar mientras el endpoint sigue montado sin interruptor», no «descubrimiento». Nota menor: el bloqueo por `duplicate` vale para las tres clases con extractor de id estable (`intake.ts:190-194`); un reintento cuyos bytes difieran cae al `sha256` (`:217`) y sí abre fila nueva.

**Consecuencia.** El despacho da de alta un token con `mnemosine webhooks create` — cuyo texto de ayuda promete «dedicated credentials that wake a restricted reader agent» (`webhooks-command.ts:53,58`) e imprime `Endpoint: POST /v1/ai/webhooks/<name>` sin una sola advertencia — y lo entrega al banco del cliente. El router está montado incondicionalmente (`index.ts:169`), sin la bandera de apagado que sí protege a la superficie pública (`PUBLIC_VERIFICATION_ENABLED`, `index.ts:152`), así que responde 200 `{"status":"received"}` a cada notificación de cargo. Nadie despierta al lector; el reintento del banco recibe 200 `{"status":"duplicate"}`; y como el cuerpo nunca se guardó, ni siquiera un drenador futuro podrá recuperar lo perdido. A los tres meses el cliente pide la conciliación bancaria: faltan todos los movimientos del canal, `webhooks deliveries` muestra filas en `'received'` que no se pueden reprocesar, y no hay ni un cuerpo crudo del que reconstruirlos — el despacho tiene que pedirle al banco un reenvío histórico completo, o rehacer a mano el periodo. El costo no es sólo el trabajo rehecho: es haberle dicho «recibido» al emisor, con acuse HTTP, sobre datos contables que se destruyeron en ese mismo instante.

---

## La API REST y las superficies que no son el CLI — **SOSTIENE**

**Razón.** La comprobación que lo tumbaría no lo tumba: `getPolicy` aparece dos veces en src/services/accounting/posting.ts (import en :3, llamada en :319) y la única llamada vive dentro de `postJournalEntry`, en el bloque 317-334 guardado por `if (!entry.source_type && entry.created_by === userId)`; la rama `if (options?.autoPost)` (:176-231) valida, bloquea periodo, hace el UPDATE a 'posted', escribe account_balances y atesta sin consultar nada, y `grep -rn getPolicy src/api` no devuelve nada. La ruta llega ahí exactamente como dice el titular: src/api/rest/routes/journal-entries.ts:63 declara `auto_post`, :180 lo destructura y :196 lo pasa como `{ autoPost: auto_post, reference }` SIN `sourceType` — es decir, produce una póliza MANUAL (source_type nulo), que es justo la clase que la política gobierna según pending-catalog.ts:439-460, mientras los demás llamadores de autoPost del árbol sí marcan origen (ar-ap-posting.ts:140,200 = invoice/bill; draft-service.ts:407-410 = ai_draft). El router va montado sin bandera en src/index.ts:192 detrás de `authenticate` y `requirePermission('journal_entries:create')` (a diferencia de GraphQL, apagado en :234), y `contador` tiene ese permiso (src/auth/roles.ts:116-125). Las citas del enunciado se sostienen tras el PR 7 con deriva de ±1 línea: entry-command.ts:69-74 dice literalmente que el `auto_post` del REST «is deliberately not exposed», y docs/cli-command-catalog.md:389,449 repiten la doctrina.

**Consecuencia.** Un despacho con la política en `exigir` cree tener control de cuatro ojos sobre la póliza manual y no lo tiene: el mismo contador que recibe SOD_QUIEN_CREA_NO_POSTEA en `entry post` (y también en POST /v1/journal-entries/:id/post, que sí llama a postJournalEntry desde journal-entries.ts:213) manda el mismo cuerpo con `auto_post:true` y recibe 201 con status 'posted', saldos movidos, y dos filas en audit_log —'create' (posting.ts:166-173) y 'post' (:194-204)— con el mismo user_id y `reason` nulo. En `alertar` la pérdida es aún más silenciosa: la nota `notaSoD` que la rama buena escribe en la fila de auditoría (:332, :363) simplemente no existe, así que ni siquiera queda constancia de la coincidencia. El mayor es inmutable desde la 041: el único remedio es una reversa manual. Ante un auditor externo esto no es un bug menor sino la anulación del control que el propio panel publica como vigente — el argumento de venta del producto es la auditabilidad, y aquí el registro de auditoría afirma que el control se respetó cuando nunca se consultó. Nada lo prueba: el test de punta a punta del maker-checker (tests/integration/f01-catalogo-asiento-mayor.int.spec.ts:216-235) sólo ejercita postJournalEntry, y los cuatro casos de autoPost de tests/accounting/posting-create.spec.ts:130-167 miran validación, saldos y atestación. Aviso para quien lo arregle: period-close.ts:532-540 y :548-569 crean pólizas 'closing' con autoPost y sin sourceType, creador=posteador a propósito; meter el mismo getPolicy en la rama sin exentar por tipo de asiento congelaría el cierre duro bajo `exigir`.

---

## Dependencias, build y las puertas de calidad — **SOSTIENE-CON-MATIZ**

**Razón.** Verifiqué las tres citas y todas dicen lo que el titular afirma: `vitest.config.ts:8` excluye `tests/integration/**`; `vitest.integration.config.ts` son 15 líneas sin un solo bloque `coverage`; `vitest.config.ts:36-49` declara umbral para 4 rutas, de las cuales sólo 3 son del motor (`posting.ts`, `validation.ts`, `ar-ap-posting.ts`) — la cuarta es `src/utils/sequence.ts` —, y `find src/services/accounting -name '*.ts'` da exactamente 16. Corrí la comprobación que el propio lente nombra como asesina: vacié `src/services/accounting/iva-ppd-reclass.ts` de 291 líneas a `export const GUTTED = true;` y corrí el job `unit` entero — `npm test` → exit 0, `npx vitest run --coverage` → **exit 0**, 143 archivos / 2205 pruebas en verde. Los números de hoy coinciden con los publicados (ledger-checks 33,17 y sequence 79,12 exactos; period-close 6,93 vs 6,77 publicado, deriva por pruebas añadidas desde `cfe40c6`). El PR 7 de ESLint no tocó `vitest.config.ts` (último commit que lo tocó: `ab12faa`), así que el árbol movedizo no lo mata; sólo desplazó la cita del `--coverage`, que hoy es `.github/workflows/ci.yml:74` y no `:59`. Y encontré un agravante que el lente no vio: `src/plan/criterios.ts:348-359` certifica «La cobertura del motor contable tiene trinquete por archivo» con la regla `archivos >= 3`, y E0.1 está en el `--exigir` de `ci.yml:109` — o sea que el job `plan` bendice como cerrado un trinquete que cubre 3 de 16. El matiz es de enunciado, no de hecho: «no pueden mover la puerta en ninguna de las dos suites» se lee como «ese código está sin proteger», y no lo está — el job `integration` (`ci.yml:146`) sí corre `tests/integration/iva-ppd-reclass.int.spec.ts` y compila/falla sobre esos módulos. Y el escenario de fallo que el informe publica —«alguien rompe una rama que la integración no cubre»— tampoco lo evitaría el umbral que implícitamente pide: un trinquete detecta pérdida de cobertura, no ruptura de código ya cubierto.

**Matiz.** El titular debe decir que lo ciego es el TRINQUETE DE COBERTURA, no la CI entera, y cambiar el escenario. Redacción defendible: «El trinquete de cobertura cubre 3 de los 16 archivos del motor contable; los 13 restantes —entre ellos `period-close.ts` (6,93 %) y `ledger-checks.ts` (33,17 %)— pueden perder toda su prueba sin que ninguna puerta lo diga, porque su cobertura se mide en la suite que los excluye y la que sí los ejecuta no mide cobertura. El agujero no es romper una rama no cubierta (eso ningún trinquete lo atrapa): es BORRAR o marcar `.skip` la prueba de integración, tras lo cual `unit`, `integration` y `plan` salen los tres en verde.» Y hay que añadir los dos documentos que mienten sobre sí mismos, que es la parte más grave y está sepultada: `vitest.config.ts:25` dice «Dos ausencias deliberadas» cuando las ausencias son trece, y `src/plan/criterios.ts:354-358` da por cumplido «trinquete por archivo» con el umbral `archivos >= 3` sobre 16 — dos gestos que hacen creerse protegido a quien lee la configuración y el estado del plan.

**Consecuencia.** El día que alguien refactoriza el cierre de periodo y, para que la corrida verde salga rápido, borra o marca `.skip` en `tests/integration/period-close.int.spec.ts` o `tests/integration/iva-ppd-reclass.int.spec.ts`, la CI completa sale verde y nadie firma nada: `unit` pasa, `npx vitest run --coverage` sale exit 0 —lo comprobé con el archivo vaciado—, `integration` pasa con menos pruebas, y `npm run plan:status --exigir=...,E0.1,...` sigue diciendo que el motor contable «tiene trinquete por archivo». El PR entra sin un solo rojo. `reclasificarIvaPpd` toca, según `scripts/reclasificar-iva-ppd.ts:132`, asientos ya posteados y con ellos periodos que ya no se pueden sellar: una reclasificación de IVA PPD mal hecha sobre asientos posteados obliga al despacho a rehacer y re-presentar declaraciones ya enviadas, a reabrir y volver a sellar periodos cerrados, y a explicarle a un cliente por qué su balanza de un mes cerrado cambió. Eso es honorarios de rehacer trabajo cobrado, más un hueco en el rastro de auditoría justo en el módulo del que el sistema presume tener trinquete.

---

## Los tres documentos rectores contra el arbol de hoy — **EXAGERADO**

**Razón.** Corrí el medidor en este árbol: `Agregado desde S0.1: 31 fila(s) · 13126 líneas · 423 líneas/fila`, y ningún script publica desglose entrega/garantía (grep sobre src/plan y scripts: vacío) — o sea, la prueba que el propio lente propuso no lo tumba. Pero su cláusula más citable sí es falsa: el titular afirma que «el 390 sólo aparece rotulado Referencia fundacional», y no es cierto — 390 es también lectura del mismo medidor sobre otra ventana, la que el documento declara al renglón: docs/plan-catalogo.md:13-14 dice «se volvió a medir en septiembre sobre las 27 filas que cerraron F01 y F02, con las 10 520 líneas del tramo 5d24463…a149e62: 389,6», y la tabla por segmentos del medidor da 8471+2042 = 10513 líneas sobre 18+9 = 27 filas = 389,4. La discrepancia 390 vs 423 no es «cifra a mano contra instrumento vivo»: es agregado histórico (contaminado por el tramo de arranque 2bf6630, 625 líneas/fila, y por el segmento de Δinv=-2 que carga 114 líneas con cero filas) contra régimen reciente, con la ventana declarada. Agravante de método: §1 del Plan Maestro —el sujeto del titular y fuente de la cita «gana el medidor»— no está en el árbol (no hay más HTML que scripts/artefacto/plantilla.html y coverage/; el único «gana el medidor» del repo está dentro de los propios informes de auditoría), así que la premisa que sostiene el titular no admite verificación archivo:línea.

**Matiz.** Enunciado defendible: «El desglose 200 + 190 líneas/fila (entrega/garantía) que publican §1 y docs/plan-catalogo.md:20-22 no lo emite ningún instrumento del árbol, y los dos documentos rectores no coinciden entre sí (189 en la tabla de :21, 190 en la recomendación de :105).» Hay que retirar tres cosas: (1) que el 390 no sea lectura de medidor —lo es, sobre la ventana F01–F02 declarada en :13-14—; (2) que 423 sea «la» lectura viva frente a una cifra escrita a mano —son dos ventanas del mismo instrumento, y para presupuestar filas futuras la reciente es al menos tan defendible como el agregado, que incluye el tramo de arranque a 625 líneas/fila—; (3) la cita a §1 mientras el Plan Maestro esté fuera del repositorio: sin ese archivo, «§1 jura que gana el medidor» es afirmación sin evidencia abrible.

**Consecuencia.** Si el titular fuera cierto tal como está escrito, el costo sería dimensionar la fase 1 con 271 × 390 = ~105 700 líneas cuando el instrumento manda 423, es decir ~114 700: unas 8 900 líneas fuera de presupuesto, cerca de un sprint. Pero ese daño no aterriza: docs/plan-catalogo.md:78-80 publica esa misma cuenta declarando «unos diez sprints — el doce sprints de agosto sobrevivió con dos de holgura», así que el desvío cabe entero dentro de la holgura que el propio documento reservó, y el régimen reciente que el 390 resume corrió a 471 y 227 líneas/fila (F01 y F02), no a 423. El daño real y acotado es otro, y es el que el propio lente clasificó BAJA: el día que S2 instrumente «entrega y garantía como dos renglones» (plan-catalogo.md:104-105), el medidor tendrá que elegir entre 189 y 190 sin que ningún documento diga cuál se midió. Publicar esto como titular de ALTA gasta credibilidad de despacho en una discrepancia de ventana, y le da a Victor un argumento de una línea —abrir plan-catalogo.md:13-14— para desestimar de un golpe las otras seis vulneraciones del mismo lente.

---
