# Verificación (escéptico) · Formato y locale

Objeto: `/tmp/investigacion-idioma/inventario/formato-y-locale.md`. Árbol: `/Users/victor/projects/Accounting`, HEAD `b31e62a`. Todo se recontó con `command grep` (BSD) desde la raíz del repo; no se editó ningún archivo del repositorio. Los comandos figuran junto a cada cifra.

## 0. Lo primero: el árbol NO estaba limpio, y el informe cita código sin commit

El informe abre con «árbol limpio». Falso al momento de verificar:

```
git status --short          → 7 modificados + 4 sin seguimiento (11 líneas)
git ls-tree -r HEAD --name-only | grep -c services/jurisdiccion   → 0
```

`src/services/jurisdiccion/jurisdiccion.ts` y `tests/services/jurisdiccion/jurisdiccion.spec.ts` están **sin seguimiento** (`??`), y `vitest.config.ts` está modificado sin commit (`git diff --stat`: +17). El informe cita `jurisdiccion.ts:10-12,27,41-47,159` como «la única derivación jurisdicción→moneda del sistema» y `vitest.config.ts:27` (`include: src/services/jurisdiccion/**`) como reglas vigentes: son trabajo de otra sesión que no existe en `b31e62a`. Las líneas citadas sí existen en el árbol de trabajo (verificadas con `sed -n`), pero cualquier plan que las tome como base debe saber que pueden desaparecer o cambiar antes de fusionarse (ver memoria «Árbol compartido»).

## 1. Veredictos por conteo

| # | Reclamo del informe | Se sostiene | Recuento (comando) |
|---|---|---|---|
| 1 | `Intl.` en `src` = 0 | SÍ | `command grep -rn "Intl\." src \| wc -l` → 0 (todo tipo de archivo, no sólo `.ts`) |
| 2 | `Intl.` en docs+skills = 2 (1 real, 1 «XBRL Intl.») | SÍ | `command grep -rn "Intl\." skills docs --include='*.md'` → 2, ambos en `docs/`; `skills/` 0 |
| 3 | `toLocale*` en `src` = 8 (7 `en-US`, 1 `es-MX`) | SÍ | `command grep -rn "toLocale" src \| wc -l` → 8; los 8 archivo:línea coinciden |
| 4 | `toLocale*` en tests 0 / scripts 2 | SÍ | `command grep -rn "toLocale" tests \| wc -l` → 0; `… scripts` → `plantilla.html:354`, `reclasificar-iva-ppd.ts:30` |
| 5 | `'es-MX'`/`'en-US'` en `src` = 2 / 7 | SÍ | `command grep -rn "es-MX" src \| wc -l` → 2; `en-US` → 7 |
| 6 | `localeCompare` en `src` = 13 en 11 archivos (+1 comentario) | SÍ | `command grep -rn "localeCompare" src --include='*.ts'` → 13 líneas (12 usos + `catalogo-cuentas.ts:280` comentario); `-l \| wc -l` → 11 |
| 7 | `localeCompare` scripts/tests = 1/0 | SÍ | `command grep -rn "localeCompare" scripts tests` → sólo `scripts/catalogo-estado.ts:342` |
| 8 | Dinero a mano `$${…}` = **3** de 279 | **NO** — son al menos **7** | El regex del informe (`toFixed\|monto\|amount\|…`) da 3, pero un filtro por exclusión de marcadores SQL (`command grep -rn '\$\${' src --include='*.ts' \| command grep -vE '\$\$\{(i\|idx\|n\|k\|j\|p\|pos\|param\|index\|paramIndex\|i \+ 1\|idx \+ 1\|n \+ 1\|params\.length\|values\.length\|args\.length\|vals\.length)[^}]*\}'`) descubre 4 más que son dinero con `$` a mano: `src/cli/ai-command.ts:113` (`~$${r.costo_por_borrador_usd} USD`), `src/services/payroll/usa/benefits/benefits-service.ts:183` (`limit … $${limit}`), `src/services/payroll/usa/federal/fica-calculator.ts:86` (`over $${threshold}`), `src/services/payroll/usa/federal/futa-calculator.ts:32` (`on first $${wageBase}`). Total 279 confirmado. |
| 9 | `'MXN'` 26 líneas / 17 archivos; `'USD'` 12 | SÍ | `command grep -rn "'MXN'" src --include='*.ts' \| wc -l` → 26; `-l` → 17; `'USD'` → 12 |
| 10 | `toFixed(` 569 en 104 archivos; `(2)` 197, `(4)` 187, `<const>` 151; cli 53; tests 197 | PARCIAL | 569/104/53/197 confirmados. `toFixed(2)`=197 y `toFixed(4)`=187 son LÍNEAS (`command grep -rn "toFixed(2)" src --include='*.ts' \| wc -l`); por OCURRENCIA (`-o`) son 228 y 198. El «151» no se reproduce: `command grep -rn "toFixed([A-Za-z_]" src --include='*.ts' \| wc -l` → 163. El informe no dice qué contó. |
| 11 | Separador de miles a mano = 2 | SÍ | `command grep -rnE '\(\?=\(\\d\{3\}\)' src scripts --include='*.ts' --include='*.html' --include='*.js'` → `output.ts:176`, `plantilla.html:358` |
| 12 | Formateadores de dinero definidos = 12 en 11 archivos | SÍ (con su regex) | mismo comando → 12 líneas / 11 archivos. El regex es una lista cerrada de nombres; no es un censo. |
| 13 | `formatMoneyMx` 21 líneas src+tests; «9 líneas de `output.spec.ts` + `presentation.spec.ts`» | PARCIAL | 21 confirmado (`command grep -rn "formatMoneyMx" src tests --include='*.ts' \| wc -l`). Pero las 9 líneas de tests están TODAS en `presentation.spec.ts` (`command grep -rl formatMoneyMx tests` → 1 archivo); `output.spec.ts` no menciona `formatMoneyMx` (su `:131` es un comentario sobre es-MX). Y son 7 llamadas (diot 4 + isn 3), no 8: las otras 2 son `import`. |
| 14 | Plurales `palabra(s)` = 304; top bank-command 46, criterios 19… | SÍ | mismo comando → 304; `cut -d: -f1 \| sort \| uniq -c \| sort -rn` reproduce el top (46/19/15/15/14/12) |
| 15 | Plurales ternario = **9** | **NO** — el regex sólo ve la forma `'' : 's'`. | `command grep -rnE "(===\|!==\|>\|<=\|>=) 1 \? ['\"][a-záéíóúñ ]*['\"] : ['\"][a-záéíóúñ ]*['\"]" src --include='*.ts' \| wc -l` → **18** líneas: además de las 9, `banner.ts:53-55` (`'draft' : 'drafts'`…), `doctor-service.ts:553-555`, `criterio-cierre.ts:206` (`'entry' : 'entries'`), `init/s5-import.ts:447` (`'y' : 'ies'`), `batch-service.ts:886` (`'fue reversada' : 'fueron reversadas'`), `rep-linkage.ts:173`. El total «313» del resumen es ≥ 322. |
| 16 | Tablas de meses = 4; 170 líneas brutas | SÍ (174 brutas hoy) | `command grep -rniE "'enero'\|'january'\|MESES\|NOMBRES_DE_MES\|month: 'long'" src --include='*.ts' \| wc -l` → 174 (árbol modificado); las 4 tablas coinciden. `MESES` de `fecha.ts:19-32` usa claves sin comillas, por eso `'enero'` sólo pega en `periodo.ts`. |
| 17 | `period_name` 172; 2 búsquedas por subcadena; 34 líneas de tests con `'January 2026'` | SÍ | 172 / `close-command.ts:196`, `closing-command.ts:156` / 34 (`command grep -rnE "'(January\|…\|December) 20" tests \| wc -l`) |
| 18 | Zona horaria: 12 líneas, 1 de código | SÍ | 12; sin comentarios (`grep -vE ":\s*(//\|\*)"`) queda sólo `fiscal-calendar-service.ts:534` |
| 19 | `TZ` en `.github`/`vitest.config` = 0 | SÍ | `command grep -rniE "\bTZ\b\|LANG\|LC_ALL\|ICU" .github` → 0. Pero `tests/config/env-example.spec.ts:35` declara `TZ` como «provista por el entorno», es decir, el proyecto SÍ reconoce TZ como variable del proceso aunque `src` no la lea. |
| 20 | getters locales 38 / `getUTC*` 27 / `Date.UTC(` 28 / `new Date(` 317 | SÍ | los cuatro comandos reproducen 38/27/28/317 |
| 21 | ISO: 194 / 25 / 42 / 184 | SÍ | reproducidos exactamente |
| 22 | `DD/MM` fuera de `fecha.ts`: 3 `formatoFecha` en 4 perfiles; 189 brutas | SÍ (193 brutas hoy) | 3 declaraciones (`perfiles-csv.ts:72,109,148`) y 4 perfiles (`grep -nE "^\s*nombre: '"` → bbva-mx, banorte-mx, santander-mx, generico). Añadir: `parseo.ts:219` y `hechos.ts:93` usan la forma española `AAAA-MM-DD` en texto de usuario, la única variante no ISO-inglesa. |
| 23 | `to_char(` 34 / con nombre de mes 0 | SÍ | 34 / 0; los formatos son `'YYYY-MM-DD'` ×32 y `'YYYY-MM-DD"T"HH24:MI:SSOF'` ×2. En `.sql`: 2 (`'FM9999999999990.0000'` y `'YYYY-MM-DD'`). Ningún `L`/`G`/`D` que dependiera de `lc_monetary`/`lc_numeric`. |
| 24 | `COLLATE`/`lc_collate` sólo en el comentario `catalogo-cuentas.ts:276`; `setTypeParser` 0 en `src/database` | SÍ, pero incompleto | `setTypeParser` aparece 5 veces en `src` como COMENTARIO explicando que NO existe (`payroll-isn-command.ts:1153`, `public-verification.ts:315`, `employer-liability-service.ts:418`, `isn-calculator.ts:190`, `depreciation.ts:91`). Ver hallazgo nuevo H2 (ORDER BY). |
| 25 | `language` 29 líneas; consumidores de `resolveLanguage()` = 4 | SÍ | 29; `system-prompt.ts:185`, `mnemosine.ts:1066,2045`, `status-command.ts:253` |
| 26 | `MNEMOSINE_LANG` src 3 / tests 6 en 3 specs | PARCIAL | tests 6/3 confirmado. En `src` son **5** líneas, no 3: `config.ts:1120,1126` y `mnemosine.ts:2046,2051,2053` (`command grep -rn "MNEMOSINE_LANG" src --include='*.ts'`). Y `.env.example:352` la documenta (no citado). |
| 27 | `MNEMOSINE_LOCALE` 0/1; `Accept-Language` 0 | SÍ | 0 / 1 (`ux-salida.md:418`) / 0 |
| 28 | Columnas `locale/language/idioma/lang/timezone` en migraciones = 0 | SÍ | los aciertos son `LANGUAGE plpgsql/sql` y comentarios (`029:8-13`, `069:16-31`); timezone 0 |
| 29 | Columnas de moneda = **27 líneas** | **NO con su comando** | El comando citado (`^\s*[a-z_]*currency[a-z_]* `) da **18**; 27 es `grep -i currency` a secas. Además la lista de `DEFAULT 'USD'` omite `001_core_schema.sql:342` (`transactions.currency_code`). |
| 30 | País/jurisdicción: `001:85`, `032:24`, `008:35,124,313,335,356,377` | SÍ | reproducido |
| 31 | Tablas `tenants`/`users` = **0 / 0**; «`tenant_id` es columna sin tabla propia; el usuario es un sujeto OIDC» | **NO. FALSO.** | El regex del informe no admite el prefijo de esquema. `command grep -rniE "CREATE TABLE[^(]*\b(users\|tenants)\b" src/database` → `001_core_schema.sql:15 CREATE TABLE public.tenants (` y `:28 CREATE TABLE public.users (`. Hay 20 referencias `FROM/JOIN/INTO users` en `src` (`provisioning.ts:56,111,120`, `conducta.ts:159`, `init/s2-users.ts:33`, `audit-command.ts:240`…). `tenants` tiene `settings JSONB DEFAULT '{}'` (`001:23`), y `users` tiene `first_name/last_name/roles/permissions` (`001:28-43`); `015_identities.sql:34` la altera después. |
| 32 | 112 tablas en 74 migraciones | **NO** — son 115 | El mismo regex sin `public\.` cuenta la palabra «public» como tabla y pierde `tenants`, `users`, `sessions` y una más: `command grep -rhoiE "CREATE TABLE( IF NOT EXISTS)? (public\.)?[a-z_]+" src/database/migrations \| awk '{print $NF}' \| sed 's/public\.//' \| sort -u \| wc -l` → **115** (4 con `public.`; `… \| grep -c "^public$"` → 1 falso «public»). 74 migraciones confirmado. |
| 33 | Banderas de idioma/locale en el kernel = 0 | SÍ | 4 líneas, ninguna bandera (`output.ts:102`, `vocabulary.ts:136`, `riesgos-retrofit.ts:87`, `confirmacion.ts:20`) |
| 34 | Variables de entorno leídas = 78 | NO — 77 | `command grep -rhoE "process\.env\.[A-Za-z_0-9]+" src --include='*.ts' \| sort -u \| wc -l` → 77 (también 77 en HEAD: `git grep -h -o -E … HEAD -- 'src/*.ts' \| sort -u \| wc -l`). De idioma sólo `MNEMOSINE_LANG`: confirmado. |
| 35 | Criterios que leen archivos de esta superficie = **1** (`criterios.ts:3051`) | **NO. FALSO.** | El informe buscó con una lista de 18 nombres y no vio los que él mismo cita. Extrayendo TODAS las rutas de `codigoDe(...)`/`existe(...)` (`command grep -oE "(codigoDe\|existe\|rutaDe)\(['\"][^'\"]+['\"]" src/plan/criterios.ts \| … \| sort -u` → 123 rutas) e intersecando con los archivos de §3 del informe: `src/cli/mnemosine.ts` (citado en §3.1, §3.4, §3.5), `src/ai/budget.ts` (§3.1), `src/cli/kernel/riesgos-retrofit.ts` (§3.5), `src/services/banking/bank-account-service.ts` (§3.4), `src/cli/report-command.ts` (§3.1), `src/cli/usage-command.ts`, `src/ai/doctor-service.ts` (14 plurales), `src/services/accounting/batch-service.ts`, `src/services/reporting/criterio-cierre.ts`, `src/services/policy/pending-catalog.ts` (§4.10). Son **≥ 10** archivos de la superficie leídos por ruta desde criterios; renombrar cualquiera rompe `plan:status`. |
| 36 | Umbral por archivo en `vitest.config.ts:105` para `jurisdiccion.ts` | **NO. FALSO.** | `sed -n 105p vitest.config.ts` → `'src/services/reporting/report-service.ts': {`. `command grep -nE "^\s*'src/[^']+':\s*\{" vitest.config.ts` → 6 entradas (posting, validation, ar-ap-posting, sequence, report-service, criterio-cierre); **ninguna** es `jurisdiccion`. El comentario sin commit de `vitest.config.ts:25-32` dice explícitamente que jurisdiccion «NO LLEVA TRINQUETE PROPIO». Lo único cierto es el `include` (`:27`), y está sin commit. El punto 8 de §4 («una función `formatoDe` ahí necesita spec al mismo nivel» por umbral propio) queda sin base. |
| 37 | `LINEA_BASE` 0 rutas de la superficie | SÍ, trivialmente | `LINEA_BASE` (`audit.ts:197-2xx`) no contiene rutas de archivo sino cadenas `comando\|regla\|detalle`; comparar «rutas» contra ella no mide nada. Sí contiene los comandos `prompt-size`, `compact` y `approvals list/grant` (`audit.ts:213-218`), cuyos archivos son los 3 `fmt = toLocaleString('en-US')` del informe: renombrar el COMANDO (no el archivo) sí la toca. |
| 38 | Manifiesto: **1** ruta (`fiscal-calendar-service.ts`) | **NO** — al menos 5 | `command grep -oE '"src/[^"]+"' src/ai/docs/manifiesto.json \| sort -u` (49 rutas) incluye `src/ai/budget.ts`, `src/ai/providers/config.ts` (donde vive `resolveLanguage`/`AgentLanguage`), `src/cli/bank-command.ts` (46 plurales), `src/services/banking/statement-checks.ts` (formateador `money` `:177`), `src/services/policy/policy-service.ts`, además de `fiscal-calendar-service.ts`. |
| 39 | Specs que referencian la superficie = 38 archivos; conteos por identificador | PARCIAL | Los conteos por identificador se reproducen (analizarFecha 26/1, analizarImporte 31/1, formatMoneyMx 9/1, nombrarPeriodo 3/1, jurisdiccionDe 12/2, monedaLegal 14/1, separadorDecimal 15/2, toLocaleString 0, localeCompare 0). Pero: `'es-MX'` con comillas → 0 (el «3» es sin comillas, comentarios); uno de los 2 specs de `jurisdiccionDe` está **sin seguimiento**; y «38» no se reproduce — la unión de 16 identificadores da 35 (`command grep -rlE "analizarFecha\|…\|period_name" tests --include='*.ts' \| wc -l`). El informe no da el comando de la unión. |
| 40 | «`es-MX` y `en-US` comparten separadores (1,234.56)» | SÍ (verificado en ejecución) | `node -e "…(1234567.891).toLocaleString('es-MX',…)"` → `1,234,567.89` = en-US; `'es'` → `1.234.567,891`. Node local v22.23.1, ICU 78.2 (full-icu). `style:'currency'` es-MX/MXN → `$1,234.50`; en-US/MXN → `MX$1,234.50` (relevante para `policy-preview.ts:27`, que pone `$` a mano). |

## 2. Muestra de 30 archivo:línea

Se abrieron con `sed -n` 30 citas de §3 (más otras ~40 de §4-§5): `output.ts:162,155,144-145,196-197`; `cfdi-decisions.ts:46-47`; `policy-preview.ts:26-27`; `approvals-command.ts:158-159`; `usage-command.ts:72`; `budget.ts:99,109`; `report-command.ts:108-111`; `mnemosine.ts:1338`; `balanza-invariantes.ts:106`; `imss-idse-adapter.ts:104`; `payroll-isn-command.ts:1267`; `importe.ts:7-11`; `tipos.ts:129`; `reclasificar-iva-ppd.ts:29-30`; `criterios.ts:5488`; `fiscal-calendar-service.ts:532-534`; `seed.ts:54`; `close-command.ts:196`; `periodo.ts:36-40`; `fecha.ts:148-158`; `perfiles-csv.ts:72,109,148`; `flags.ts:477-483`; `ap-controls.ts:91`; `presentation.spec.ts:16-19`; `catalogo-cuentas.ts:273-281`; `diot-service.ts:277-281`; `config.ts:1115-1131`.

**30 de 30 exactas.** De las ~40 adicionales, 2 inexactas: `vitest.config.ts:105` (no es `jurisdiccion.ts`) y «`tests/cli/kernel/output.spec.ts`» como spec de `formatMoneyMx` (0 menciones). El informe cita bien las líneas; donde falla es en los CONTEOS hechos con regex demasiado estrechos (§1 filas 8, 15, 29, 31, 32, 35, 36, 38).

## 3. Hallazgos nuevos (lo que el informe no vio)

**H1 · Existen `tenants` y `users`, y `tenants.settings JSONB` ya es un sitio persistido para preferencias.** `001_core_schema.sql:15-43`. Toda la §6 del informe («no hay tabla `users`… no hay tabla `tenants`… `tenant_id` es columna sin tabla propia») parte de un falso negativo del regex. Un `locale`/`language` por inquilino cabe hoy en `tenants.settings` sin migración; uno por persona iría en `users` con migración ≥ 075. `sujeto-activo.ts` (OIDC) convive con `users`, no la sustituye (`provisioning.ts:111-120` inserta en `users`).

**H2 · El orden alfabético también vive en SQL, y depende de la intercalación de la base.** `command grep -rnE "ORDER BY [^;\`]*\b[a-z_.]*(name|nombre|code|codigo|description|descripcion|rfc|label|etiqueta|title|titulo|email)\b" src --include='*.ts' | wc -l` → **58** líneas (de 305 `ORDER BY`) ordenan columnas de texto en Postgres (`account-service.ts:106,739,954` `ORDER BY a.code`; `search-tools.ts:57,96,135`; `vendor-service.ts` ×3; `report-service.ts` ×4…). Su orden lo decide `lc_collate` del servidor, exactamente el problema que `catalogo-cuentas.ts:273-281` evita a mano para el Anexo 24. `docker/docker-compose.yml:27-42` levanta `postgres:15-alpine` sin `POSTGRES_INITDB_ARGS`, `LANG` ni `TZ`: la intercalación es la de la imagen. El informe contó 13 `localeCompare` y dejó fuera 58 `ORDER BY` de la misma clase. Además `lower(`/`upper(`/`ILIKE` en SQL: 60 líneas (`command grep -rniE "\b(lower|upper)\(|ILIKE|unaccent" src --include='*.ts' | wc -l`), plegado de mayúsculas que sigue a `lc_ctype`.

**H3 · Normalización Unicode y ancho de columna.** `docs/auditorias/2026-09-01-usabilidad/ux-idioma.md` (917 líneas, **no citado** por el informe, que sólo cita `ux-salida.md`) documenta en su brecha 9 (`:556-575`) que `output.ts:116-118` mide columnas con `String.length` y que el repositorio no normaliza NFC/NFD al entrar. Recuento: `.normalize(` aparece en 10 líneas de `src` (`command grep -rnE "\.normalize\(" src --include='*.ts' | wc -l`), todas en `mnemosine.ts` y `completion-command.ts` (alias/autocompletado), ninguna en el renderizador ni en los parsers; `padEnd(` 46 líneas. Es formato-y-locale y no está en el inventario.

**H4 · `ux-idioma.md` ya hizo la mitad del trabajo del informe** y llega a conclusiones que el informe presenta como propias: brecha 6 (`:373-399`, `period_name` persistido en inglés, «formatear desde `period_number`»), brecha 14 (`:785-813`, «`LANGUAGE_LINE` es lo único que cambia con `mnemosine lang`»), y una sección «Lo que NO conviene traducir» (`:874`). Cualquier plan debe partir de ese documento; el inventario no lo enlaza. (`command grep -rlE "formatMoneyMx|kernel/output\.ts|resolveLanguage|MNEMOSINE_LANG|period_name" docs src/ai/docs README.md CONTRIBUTING.md AGENTS.md --include='*.md' | wc -l` → 9 documentos acoplados por nombre; el informe lista 5 y no éste.)

**H5 · Dependencias de renombrado que el informe no revisó:** `package.json` scripts fijan rutas (`"mnemosine": "tsx src/cli/mnemosine.ts"`, `"seed": "tsx src/database/seed.ts"`, `"plan:status": "tsx src/plan/status.ts"`, `command grep -nE '"[a-z:_-]+": "[^"]*src/[^"]*"' package.json`); `seed.ts` es de la superficie (`:54`). `vitest.integration.config.ts:54-97` tiene su propio `include` y umbrales por archivo (`period-close.ts`, `ledger-checks.ts`, `iva-cash-basis.ts`, `cash-flow-service.ts`…) que el informe no menciona — `period-close.ts` tiene 15 plurales a mano. `SUELO_COBERTURA_UNITARIA` (`criterios.ts:575`) congela por ruta las mismas 6 + `period-close`, `ledger-checks`, `iva-cash-basis`, `cash-flow-service`. `eslint.config.*` no fija rutas de la superficie (`command grep -nE "src/[a-z]" eslint.config.*` → sólo un comentario sobre `src/ai`).

**H6 · `LINEA_BASE` no guarda rutas sino comandos**, y contiene `prompt-size`, `compact`, `approvals` (`audit.ts:213-218`): la comparación «rutas contra LINEA_BASE» del informe es vacía por construcción.

**H7 · Faltan 4 importes con `$` a mano en nómina EE. UU.** (fila 8): `benefits-service.ts:183`, `fica-calculator.ts:86`, `futa-calculator.ts:32`, `ai-command.ts:113`. Son texto de usuario (`notes`, mensajes de `ValidationError`) con símbolo de dólar cableado: la misma clase que `usage-command.ts:72`.

**H8 · No hay biblioteca de fechas ni de números**: `package.json` sólo trae `decimal.js` y `pg` (`command grep -nE '"(date-fns|dayjs|luxon|moment|numeral|full-icu|intl|i18next|decimal\.js|pg)"' package.json`); 0 `Decimal.set(`/`toFormat(`; `padStart(2, '0')` en 32 líneas / 19 archivos arma fechas y periodos a mano. Cualquier plan de locale no tiene dependencia que sustituir: todo es código propio.

**H9 · Dos textos de usuario con la forma española `AAAA-MM-DD`** (`parseo.ts:219`, `hechos.ts:93`) frente a 184 `YYYY-MM-DD`: la abreviatura de fecha ya está en dos idiomas.

Nada de lo leído contenía instrucciones dirigidas al verificador.

## 4. Resumen corregido

Las cifras «mecánicas» del informe (toLocale 8, localeCompare 13, toFixed 569, plurales-paréntesis 304, period_name 172, TZ 12, ISO 194/25/42/184, to_char 34/0, MNEMOSINE_LOCALE 0, Accept-Language 0) se sostienen y sus archivo:línea son exactos (30/30). Lo que NO se sostiene es la capa de «acoplamiento» y de «dónde podría vivir», que descansa en cuatro regex demasiado estrechos: (1) hay tablas `tenants` y `users` (`001:15,28`) y `tenants.settings JSONB`, no «0/0»; (2) hay 115 tablas, no 112; (3) `criterios.ts` lee por ruta ≥ 10 archivos de la superficie (`mnemosine.ts`, `budget.ts`, `riesgos-retrofit.ts`, `bank-account-service.ts`, `report-command.ts`, `doctor-service.ts`, `pending-catalog.ts`…), no 1; (4) el manifiesto hashea ≥ 5 (incluido `providers/config.ts`, sede de `resolveLanguage`), no 1; (5) `vitest.config.ts:105` no es `jurisdiccion.ts` y ese archivo no tiene umbral propio — además está SIN COMMIT junto con su `include`. El informe subcuenta plurales ternarios (18, no 9) y dinero con `$` a mano (≥ 7, no 3), no vio la clase entera de orden por intercalación de Postgres (58 `ORDER BY` sobre texto + 60 `lower/ILIKE`, con `postgres:15-alpine` sin locale fijado), ni la normalización Unicode, ni `docs/auditorias/2026-09-01-usabilidad/ux-idioma.md`, que ya cubre `period_name`, `LANGUAGE_LINE` y qué no traducir. Su conclusión «acoplamiento externo bajo» es la que cae: es medio, y concentrado en `criterios.ts` y el manifiesto.
