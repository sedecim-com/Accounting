# Verificación escéptica — superficie API (REST + GraphQL)

Verifica `/tmp/investigacion-idioma/inventario/api.md`. Raíz `/Users/victor/projects/Accounting`. Ningún archivo del repositorio fue editado. Artefactos de esta verificación: `/tmp/investigacion-idioma/verificacion/api-muestra-citas.txt` (las 92 citas comprobadas, formato `archivo|rango|regex`) y `api-codigos-25efb18.txt` (las 113 llamadas a `AccountingError` con archivo:línea:código sobre el commit congelado).

Nota de seguridad: nada de lo leído contenía instrucciones dirigidas al agente.

## 0. Sobre qué árbol se contó

El informe dice «rama `instrumentos-que-mienten-segun-la-maquina`» (commit `25efb18`). El árbol de trabajo real está en `j0-1-la-jurisdiccion-deja-de-ser-un-booleano` (`b31e62a`) con cambios sin comprometer (`git status --short`: `src/services/accounting/iva-cash-basis.ts`, `iva-ppd-reclass.ts`, `pais-contable.ts`, `vitest.config.ts`, `src/ai/doctor-service.ts`, más tres specs). Para contar sobre algo reproducible exporté el commit que el informe nombra:

```
git archive 25efb18 | tar -x -C $SCRATCH/arbol      # cwd de todo lo que sigue
git diff --stat 25efb18 b31e62a -- src/api src/utils src/services src/plan src/cli src/ai docs/openapi.json vitest.config.ts tests
```

Entre los dos commits sólo cambian `src/plan/criterios.ts` (+88 líneas, todas a partir de la 6535: `git diff 25efb18 b31e62a -- src/plan/criterios.ts | grep '^@@'` → `@@ -6535,6 +6535,94 @@`) y tres specs. `src/api`, `src/utils`, `src/services`, `src/cli`, `docs/openapi.json` son idénticos en ambos. Lo que NO es idéntico es el árbol de trabajo: `git diff --stat 25efb18 -- src/services vitest.config.ts` muestra `iva-cash-basis.ts` (+27), `pais-contable.ts`, `iva-ppd-reclass.ts` y `vitest.config.ts` (+17) sin comprometer.

**El informe contó sobre el árbol de trabajo, no sobre el commit que nombra.** Tres pruebas: (1) su TSV cita `iva-cash-basis.ts:232` y `:607`; sobre `25efb18` el extractor da `:231` y `:582` (`diff <(cut -f1-4 cadenas.tsv) <(cut -f1-4 api-cadenas.tsv)` → sólo esas dos filas). (2) Sus umbrales de `vitest.config.ts` (`posting.ts:82 … criterio-cierre.ts:112`) no existen en `25efb18` (`grep -nE "^\s*'src/[^']+':" vitest.config.ts` → 75, 78, 81, 84, 88, 95) ni en el árbol de trabajo de ahora (92, 95, 98, 101, 105, 112): es un estado intermedio de un archivo que otra sesión sigue editando. (3) Su top de códigos en pruebas (`SOD_QUIEN_CREA_NO_POSTEA` 10, `INTERNAL_SERVER_ERROR` 3) sólo se reproduce sobre `tests/` del árbol de trabajo; sobre `25efb18` da 9 y 1.

## 1. Conteos, uno por uno

| # | Afirmación | Comando de verificación (cwd = árbol congelado `25efb18`) | Resultado | Veredicto |
|---|---|---|---|---|
| C1 | `src/api`: **37 archivos**, 9 929 líneas | `find src/api -type f \| wc -l` · `find src/api -type f -name '*.ts' \| xargs wc -l \| tail -1` | **36** (todos `.ts`; `find src/api -type f -not -name '*.ts'` = 0), 9 929 | **Refutado** en archivos (36) |
| C1 | «13 routers en `src/api/rest/routes/`» | `ls src/api/rest/routes \| wc -l` · `grep -oE "from '\./routes/[a-z-]+" src/api/rest/montajes.ts \| sort -u \| wc -l` | **17** archivos y `montajes.ts` importa 17 | **Refutado** (17) |
| C1 | 9 middleware; 414/434/404/493 líneas; GraphQL = 4 archivos | `ls src/api/rest/middleware \| wc -l` · `wc -l` · `find src/api/graphql -type f` | 9; 414/434/404/493; `errores.ts`, `permisos.ts`, `resolvers/index.ts`, `schemas/schema.ts` | Confirmado |
| C1 | «no existe `src/services/**/errors`» | `find src/services -type d -name 'errors*'; find src -name 'errors*.ts'` | sólo `src/utils/errors.ts` | Confirmado |
| C2 | 113 llamadas a `AccountingError`, 59 códigos distintos | el `perl -0ne` del informe → `wc -l`; `cut -d: -f3 \| sort -u \| wc -l`; `diff` contra `api-codigos-distintos.txt`; `grep -rhoE "new AccountingError\(" src \| wc -l` | 113; 59; `IGUAL`; 113 (ninguna llamada con código no literal) | Confirmado |
| C2 | «desde **30 archivos de `src/services`**» | `grep -rlE "new AccountingError\(" src \| wc -l` · `… \| grep -v '^src/services'` | **27** archivos: 26 en `src/services` + `src/cli/period-command.ts` (`:444` `PERIOD_LOCKED`, `:451` `PERIOD_NOT_CLOSED`, ambos en castellano) | **Refutado** (26, no 30; y el CLI también acuña errores de dominio, no sólo los consume) |
| C3 | 21 códigos con vocablo castellano | `grep -cE "$ES_TOK" codigos-distintos.txt` con el `ES_TOK` literal del informe; los 38 restantes revisados a ojo (`grep -vE`) | 21, misma lista; ninguno de los 38 lleva castellano | Confirmado |
| C4 | `ErrorCodes`: 10 nombres; sólo `PERIOD_CLOSED` en uso (4) | `sed -n 77,88p src/utils/errors.ts`; bucle `grep -rn "'$c'\|ErrorCodes\.$c" src --include='*.ts' \| grep -v src/utils/errors.ts \| wc -l` | 10; `PERIOD_CLOSED` 4, los otros 9 = 0 | Confirmado |
| C5 | 4 códigos literales fuera de `AppError` | `grep -rnoE "code:\s*'[A-Z_]{3,}'" src/api` | `error-handler.ts:35`, `rate-limiter.ts:46`, `public-verification.ts:50,98` | Confirmado como conteo de `code:`; **incompleto como censo de salidas a mano** (H1) |
| C6 | 3 subclases con código propio | `grep -rnE "class \w+ extends (AppError\|AccountingError\|ValidationError\|NotFoundError\|ConflictError)" src \| grep -v src/utils/errors.ts` | las 3 (`serializador.ts:45,52`, `pre-registration-service.ts:62`); con `extends Error` son 33 clases | Confirmado; tres de las `extends Error` llegan al cliente (H2) |
| C7 | 890 filas; `src/api` 62/15/2/38; `src/services` 117/501/6/149; `AccountingError` 37/61/1/9 | `node api-extraer-cadenas.mjs $SCRATCH/arbol > cadenas.tsv; wc -l`; los `awk` del informe | 890; 62 en / 12+3 es / 2 mixto / 38 neutro; 117/501/6/149; 37/61/1/9 | Confirmado (salvo las dos líneas de `iva-cash-basis.ts`, §0) |
| C7 | «`src/api` en inglés al 81 % (95 de 117)» | `awk -F'\t' '$1 ~ /^src\/api/ && $3=="neutro" && $4 !~ /^NotFoundError/' cadenas.tsv` | los 5 «neutro» que no son `NotFoundError` son inglés (`Insufficient permissions` ×2, `Unknown primary_chain`, `Unknown secondary chain`, `Test webhook delivery`); `xml-ingestion.ts:424` es en realidad mixto (`Insufficient permissions … Un comprobante de pago emitido…`) y sale «neutro» porque la regex ES no lleva `/i` y `Un` va en mayúscula; `tenant-context.ts:40` sale «mixto» por la palabra «token», que está en la lista EN | **Matiz**: 100 de 117 (85 %) contando neutros ingleses; el sentido no cambia |
| C8 | 8 mensajes `{ message: … }`; 4 por argumento; 0 `errorMap` | los tres greps del informe | 8, pero `webhooks.ts:70` es la carga del `test.ping`, no Zod → **7** Zod; 4; 0 | Confirmado con matiz |
| C9 | 46 `validateBody(`, 0 `validateQuery(` en rutas | `grep -rhoE "validate(Body\|Query)\(" src/api/rest/routes \| sort \| uniq -c` | 46 / 0 | Confirmado |
| C10 | 40 `type/input/enum`; 0 `"""`; 43 `#`; 19 en 322-340; 14 separadores; 8 rótulos ingleses | `grep -cE "^\s*(type\|input\|enum)\s+\w+"` = **36** (40 sólo con `scalar`); `grep -cE '^\s*"""'` = 0; `grep -cE '^\s*#'` = 43; `awk -F: '$1>=322 && $1<=340'` = 19; `grep -cE '^\s*#\s*[=\-]+\s*$'` = **16**; rótulos = 8 (`:8,90,295,343,365,439,458,484`) | 36 / 0 / 43 / 19 / 16 / 8 | **Refutado en dos cifras** (36 declaraciones, 16 separadores) |
| C11 | 83 `escribe:`; `riesgo` 46/23/14/4; 0 acentos | los greps del informe | 83; 46 escritura, 23 irreversible, 14 externo, 4 lectura; 0 | Confirmado |
| C12 | 311 797 bytes; 195 `description/summary`; 0 con marca castellana; 101 `x-escribe`; 62 con palabra funcional; 0 `message` en `zod-a-json-schema.ts` | los greps del informe; `wc -c` | 311 797; 195; 0; 101; 62; 0 | Confirmado; pero `grep -oE '"x-[a-z-]+"' docs/openapi.json \| sort \| uniq -c` da **11 claves de extensión, 10 en castellano** (H3), y `x-riesgo` publicado es 56/28/17/6 = 107, no 87 (cada declaración se publica en más de una operación) |
| C13 | 11 claves castellanas en respuestas REST | el grep del informe | 11, mismas ubicaciones | Confirmado |
| C14 | **772** aserciones de texto literal en `tests/`; 14 en `tests/api` | el grep del informe sobre `25efb18`, sobre `b31e62a` (`git grep`) y sobre el árbol de trabajo | **766** / 768 / 771; 14 en `tests/api` en los tres | **772 no se reproduce en ningún árbol**; ampliando con `toThrowError\(\|rejects\.toThrow\(` son 983, 104 con acento/ñ |
| C14 | «Inglés fijado por pruebas de la API: sólo `permisos.spec.ts:425`» | `grep -nE "mustSay" tests/api/routes/withdrawn-endpoints.spec.ts` | `:109,115,122,129,136,142` fijan **11 frases inglesas** (`/does not transmit to the IRS/i`, `/Nothing was sent/i`, `/never transmitted/i`, `/does not upload to the SSA/i`, `/does not transmit to IMSS/i`, `/does not schedule payments/i`, `/cannot complete a bank reconciliation/i`…) sobre los `NotImplementedError` de `payroll.ts:419-444`, `bills.ts:171`, `bank-reconciliation.ts:376` | **Refutado**: la prosa inglesa de los 6 endpoints retirados está clavada por prueba |
| C15 | top de códigos citados por pruebas (`FX_ORIGEN_INCOMPLETO` 12, `SOD_…` 10, `INTERNAL_SERVER_ERROR` 3…) | el grep del informe sobre `25efb18` y sobre el árbol de trabajo | `25efb18`: 12 / **9** / **1**; árbol de trabajo: 12 / 10 / 3 | Cifras del árbol en movimiento (§0); el orden se sostiene |
| C16 | consumidores de `code` fuera de la API | el grep del informe | `entry-command.ts:146` (+ `BLOCKED_CODES` `:130-139`), `bill-command.ts:1222,1500`, `ingest-service.ts:206`, `pac-router.ts:194`, `rep-pendientes.ts:150` | Confirmado |
| C17 | `criterios.ts`: 24 referencias a `src/api`, 14 rutas distintas; líneas 1470/1476/3312/3339/2331 | `grep -c "src/api"`; `grep -oE "src/api[A-Za-z0-9_./-]*" \| sort -u \| wc -l`; `sed -n` | 24; 14; exactas (idénticas en `25efb18` y `b31e62a`: el diff empieza en 6535) | Confirmado; la lista de §5.4 omite `:5247`, y `criterios.ts` también ancla `INPC_BASES_DISTINTAS` (`:4779`, sobre `src/services/fiscal/inpc/factor.ts`) y `MISSING_ROLE_ACCOUNT` (`:1995`, en prosa) (H5) |
| C17 | `audit.ts` `LINEA_BASE`: 0 rutas de `src/api` | `grep -c "src/api" src/cli/kernel/audit.ts`; bucle por los 27 emisores + `errors.ts` + `error-handler.ts` | 0; ninguno de los emisores está en `audit.ts` | Confirmado |
| C17 | `manifiesto.json`: 4 entradas de `src/api` | `grep -o '"src/api[^"]*"' src/ai/docs/manifiesto.json \| sort \| uniq -c` | `invoices.ts` ×2, `bank-reconciliation.ts` ×2 | Confirmado; pero el manifiesto hashea además **7 servicios emisores y `src/cli/entry-command.ts`** (H4) |
| C17 | `vitest.config.ts:82-112` (`posting.ts:82`, `validation.ts:85`, `ar-ap-posting.ts:88`, `report-service.ts:95`, `criterio-cierre.ts:112`) | `grep -nE "^\s*'src/[^']+':" vitest.config.ts` en `25efb18` y en el árbol de trabajo | `25efb18`: 75/78/81/84/88/95; árbol: 92/95/98/101/105/112 | **Refutado en líneas** (ninguno de los dos estados coincide; el informe leyó un intermedio); «sin umbral para `src/api`» se sostiene; omite `src/utils/sequence.ts` |
| C18 | `express/lib/request.js:179` `acceptsLanguages`; `001:23` `settings JSONB`; `001:85` `incorporation_country`; `index.ts:129/201/262-268/273-309` | `grep -n`, `sed -n`; `grep -rniE "locale\|language\|idioma" src/database/migrations` | exactos (Express 4.22.2); ninguna migración añade preferencia de idioma (los únicos hits son comentarios de `029_ai_messages_fts.sql`) | Confirmado |
| §1 | 41 archivos del grep de idioma; en `src/api` sólo `reports.ts:249` y `openapi.ts:83` | el grep del informe con `-l \| wc -l` y con `-n` sobre `src/api` | 41; en `src/api` **sólo `reports.ts:249`** — `openapi.ts:83` es `localeCompare` y `\blocale\b` no lo captura | Matiz: `openapi.ts:83` no sale del grep citado (es irrelevante para idioma de todos modos) |
| §1 | «Las cabeceras que la API ya lee son `x-request-id` (82), `x-entity-id` (2), `user-agent` (1)» | el grep del informe (sólo captura `x-*` y `req.get`) · `grep -rnoiE "idempotency-key" src/api/rest/middleware/idempotencia.ts` | 82/2/1 se sostienen; pero la API también lee `Idempotency-Key` (`idempotencia.ts:31,68`) y `authorization` | **Incompleto**: el patrón excluye las cabeceras que no empiezan por `x-` |
| §3.2 | 72 códigos, 24 castellanos (33 %) | 6 + 59 + 3 + 4; 21 + 3 | 72; 24 | Confirmado |
| §3.5 | `permisos.ts` 10 `motivo:/razón:`; `resolvers` 3 `throw new` (`:93,155,175`) | `grep -cE "(motivo\|razón\|razon):"`; `grep -nE "throw new"` | 10; 93/155/175 | Confirmado |
| §3.6 | plantillas de `summary/description` de `openapi.ts` en `:275`, **`:316-352`**, `:375` | `grep -nE "Mutating\|Read route\|declared" src/api/rest/openapi.ts` | `Read route (no declared risk class…)` y `Mutating route declared …` están en **`:181-184`**; `:316-352` son las descripciones de los esquemas de seguridad y de `x-entity-id`; `:363-375` es `DESCRIPCION` (inglés) | **Refutado en la cita** (la plantilla vive en 181-184) |
| §5.1 | docs que citan códigos: `superficies-no-cli.md:45`, `plan-cierre-brechas.md` ×7, `completion-plan.md:158` («históricos») | `grep -rnowFf codigos72.txt docs README.md AGENTS.md CONTRIBUTING.md --include='*.md' \| wc -l`; `-l \| wc -l`; `grep -rlowFf … src/ai/docs` | **129 menciones en 33 archivos**: `plan-cierre-brechas.md` 67, `cli-command-catalog.md` 6, `docs/investigacion/…/motor-contable.md` 6, `operacion-y-fallos.md` 6, `docs/wiki/Manual-Cobrar-y-pagar.md` 2, `docs/wiki/Fiscal-mexicano.md` 1, `docs/wiki/Conectores-PAC.md` 1…; y 3 manuales del agente (`src/ai/docs/system.md`, `receivables.md`, `playbooks.md`) | **Refutado por defecto**: la wiki de usuario, el catálogo del CLI y los manuales del agente son superficies vivas |

## 2. Muestra de archivo:línea

Comprobé **92 citas** con `sed -n "${rango}p" archivo | grep -qE "$patrón"` (lista en `api-muestra-citas.txt`, 79 pasan el patrón automático; las 13 restantes se abrieron a mano). Resultado tras la lectura manual:

- **Las 31 filas de la tabla §4: 31 de 31 exactas.** Donde el `throw` ocupa varias líneas (`tenant-context.ts:40`, `fiscal-calendar-service.ts:376`, `ar-ap-posting.ts:87`, `period-close.ts:755`) el informe cita la línea del `new …Error(` y el texto está una línea después; lo cuento exacto.
- **De las 61 citas de §1, §3 y §5: 55 exactas, 6 inexactas**, todas en `25efb18` y en el árbol de trabajo:
  1. `src/cli/entry-command.ts:143-149` (`translateDomainError`): la función va de **141 a 148**.
  2. `src/api/rest/openapi.ts:316-352` como sede de las plantillas `Read route…`/`Mutating route declared…`: viven en **181-184**.
  3. `src/services/xml-ingestion/pre-registration-service.ts:74` (`details.suggested_vendor`): está en **73**; `:76` (`this.code`) sí es exacta.
  4. `vitest.config.ts:82` y **5.** `:112` (y todo el rango 82-112): ni en el commit ni en el árbol de trabajo (§0).
  6. `iva-cash-basis.ts:232,607` en el TSV: **231 y 582** en el commit.

Muestra de 30 = las 30 primeras filas de §4: **30/30 exactas**. Muestra ampliada de 92: **86/92 (93 %)**; las 6 fallas tienen una sola causa (leer un árbol en movimiento) salvo la de `openapi.ts:316-352` y la de `entry-command.ts`, que son errores de cita.

## 3. Lo que el informe no vio

**H1. Más salidas a mano, y con OTRO sobre.** `grep -rnE "res\.status\((4|5)[0-9]{2}\)" src/api src/index.ts` da 8, no 3 + el 500:
- `src/api/rest/routes/ai-webhooks.ts:82` (401 `{ error: 'Unknown or disabled webhook token' }`), `:124` (415 `{ error: 'Content-Type must be application/json' }`), `:131` (400 `{ error: 'Body must be valid JSON' }`): tres respuestas con sobre `{ error }` — sin `code`, sin `meta`, en inglés (`grep -rnE "json\(\{\s*error:" src/api src/index.ts`). Ninguna prueba fija esos textos (`grep -rnE "Unknown or disabled webhook token|Content-Type must be|Body must be valid JSON" tests` = 0), así que son los únicos mensajes de la puerta que se pueden traducir sin tocar un spec — y a la vez los únicos fuera del contrato.
- `src/index.ts:150-154`: `/ready` responde 503 con `error: err.message` — el texto crudo de la excepción de `pg`, sin filtrar.
- No hay manejador 404 de ruta desconocida (`grep -nE "404|Not Found|notFound" src/index.ts` = 0): una ruta inexistente bajo `/v1` recibe el HTML de Express (`Cannot GET …`), inglés, fuera del sobre.
- `src/index.ts:143,159`: `/live` → `status: 'alive'`, `/health` → `status: 'healthy'`: valores que un monitor compara.

**H2. Mensajes que llegan al cliente y que el extractor no censó** (sólo miraba constructores de `AppError` en `src/api`, `src/services`, `src/utils`). `grep -rnE "(err|error|e)(\s+as\s+Error\))?\.message" src/api`:
- `src/api/rest/middleware/idempotencia.ts:372`: `ConflictoDeIdempotencia` (`src/services/idempotency/idempotency-store.ts:29`) → `ConflictError(err.message)`, 409, prosa castellana; no está en las 890 filas.
- `src/api/rest/middleware/auth.ts:134`: `NoAccessError` (`src/auth/provisioning.ts:33-40`, inglés: `User ${email} is authenticated but has no access to any entity…`) → `ForbiddenError(err.message)`; `:136`: `External token rejected: ${err.message}` arrastra el texto del verificador OIDC. `src/auth` no fue barrido.
- `src/api/rest/routes/ai.ts:97`: `DraftValidationError` (`src/ai/draft-service.ts:365`) → `ValidationError` con las 19 cadenas de `errors.push(…)` de `draft-service.ts` (`grep -cE "errors\.push\(" src/ai/draft-service.ts` = 19), inglés. `src/ai` no fue barrido.
- `src/api/rest/routes/xml-ingestion.ts:206,208,590`, `blockchain.ts:173`, `bank-reconciliation.ts:148`: meten `(err as Error).message` de **cualquier** excepción en cuerpos 200/201 (`errors: [{ index, status: 'error', error }]`, `Transaction ${id}: ${message}`). Es un canal por el que cualquier mensaje de `src/services` o de `pg` sale al cliente sin pasar por `errorHandler`. `DuplicateError` (`pre-registration-service.ts:37`, `CFDI already exists: ${id}`, `:155`) sale por ahí.
- `src/database/scope.ts:155`: `NotFoundError(nombreLegible(tabla), id)`; el recurso se deriva del nombre de tabla (`:179`). Plantilla de 404 fuera de `src/api`, nombre inglés generado.
- `src/cli/period-command.ts:444,451`: dos `AccountingError` en castellano acuñados en el CLI; el informe dice que el CLI sólo consume.

**H3. El contrato publicado lleva 10 claves de extensión castellanas, no 2.** `grep -oE '"x-[a-z-]+"' docs/openapi.json | sort | uniq -c`: `x-agente-permitido` 107, `x-exige-compuerta-en-vivo` 107, `x-exige-llave-de-idempotencia` 107, `x-exige-marcha-seca` 107, `x-riesgo` 107, `x-permisos-requeridos` 175, `x-escribe` 101, `x-claves-desconocidas` 71, `x-alcance-idempotencia` 45, `x-validacion-adicional` 11 (y `x-entity-id` 1, que es una cabecera). Se acuñan en `src/api/rest/openapi.ts:137-157`. `DESCRIPCION` (`openapi.ts:363-375`, inglés) las documenta por nombre en el propio contrato. Renombrarlas regenera `docs/openapi.json` y desfasa `tests/integration/g4b-ataque.int.spec.ts:86-96` y `ci.yml:166` igual que un `escribe:`.

**H4. El manifiesto del agente hashea 7 servicios emisores y el mapa de códigos del CLI.** `for f in $(cut -d: -f1 api-codigos-25efb18.txt | sort -u); do grep -q "\"$f\"" src/ai/docs/manifiesto.json && echo $f; done` → `posting.ts`, `fiscal-calendar-service.ts`, `ar-ap-posting.ts`, `iva-cash-basis.ts`, `ar/ar-controls.ts`, `pac/pac-router.ts`, `payments/payment-service.ts`. Además `grep -oE '"src/(utils|api|cli)/[^"]+"' src/ai/docs/manifiesto.json` da `src/cli/entry-command.ts` y `src/cli/bank-command.ts`: tocar `BLOCKED_CODES` (`entry-command.ts:130-139`), que es lo que §5.1 manda hacer en un renombre, dispara `corpus-manifiesto.ts --check` (`ci.yml:158`). El informe (§5.3) sólo avisa de `invoices.ts` y `bank-reconciliation.ts`. Traducir `ya está abierto.` (`fiscal-calendar-service.ts:246`, su ejemplo estrella) caduca `manuales/*.md` exactamente igual.

**H5. `criterios.ts` ancla más de lo dicho.** Además de `:1470,1476,3312,3339`: `:4779` (`/INPC_BASES_DISTINTAS/.test(factor)`), `:1995` (`MISSING_ROLE_ACCOUNT` en prosa de un fallo), y dos regex con prosa castellana sobre código fuente (`:1360` `/\$\{name\}_\$\{año\}/`, `:3347` `/sin medición/` sobre `src/ai/budget.ts`). `:5247` falta en la lista de §5.4.

**H6. Sesgo de la heurística de idioma.** EN lleva `/i` y ES no; «token», «header», «expected» están en EN aunque se usen en castellano. Infla «mixto/neutro» (`tenant-context.ts:40`, `xml-ingestion.ts:424`); no cambia la conclusión (inglés en la puerta, castellano en los servicios), pero 81 % → 85 %.

**H7. La prosa inglesa de los 6 endpoints retirados está clavada por prueba** (`withdrawn-endpoints.spec.ts:109-142`, 11 regex). §3.8 sólo veía `permisos.spec.ts:425`. También `tests/api/middleware/async-handler.spec.ts` fija fragmentos de Zod.

**H8. Vocabulario de entrada.** Lo que el informe no dijo y sí cuenta a favor del pedido: todos los `z.enum` de las rutas (`grep -rnoE "z\.enum\(\[[^]]*\]" src/api/rest/routes`, 15) son ingleses; `docs/openapi.json` no publica ningún `enum` con valor castellano (`grep -oE '"enum":\s*\[[^]]*\]' … | grep -E "[áéíóúñ]|\b(escritura|lectura|directo|indirecto|pendiente|…)\b"` = 0); los nombres de campo Zod y de `req.query` (`country entity_id include_balance include_hierarchy search status`) son ingleses; los `field:` de `ValidationError` (`'proof'`, `'format'`) también. El único vocabulario de entrada bilingüe es `?method=` (`reports.ts:256-259`), y el único de salida castellano son las 11 claves de C13 y los valores de `x-riesgo`. Los identificadores de la superficie ya cumplen el pedido; lo que no cumple es la prosa y 5 nombres de archivo (`montajes.ts`, `topes.ts`, `errores.ts`, `permisos.ts`, `idempotencia.ts`).

**H9. Logs de la API no son superficie de usuario, pero el informe los mezcla.** `trust-proxy.ts:65-70,81-85` (`aviso:`) y `idempotencia.ts:358-362` (`logger.error('El acto salió pero la llave…')`) van al log del operador; `grep -rnoE "logger\.(info|warn|error|debug)\(" src/api | wc -l` = 2. Correcto que §4 lo aclare para `trust-proxy`; el `aviso:` de `trust-proxy.ts:81` entra igualmente en las 117 cadenas de `src/api` del TSV como «texto de usuario renderizado», así que las 117 son 115 de cliente + 2 de log.

## 4. Resumen corregido

La superficie API son **36** archivos y 9 929 líneas en `src/api` (**17** archivos de ruta, no 13), más los mensajes de 113 `AccountingError` en **26** archivos de `src/services` y 1 del CLI (`period-command.ts:444,451`). El sobre `{errors:[{code,message,field,details}],meta}` NO es único: `ai-webhooks.ts:82,124,131` responden `{ error }` sin código ni `meta`, `/ready` (`index.ts:150-154`) filtra el mensaje de PostgreSQL, `xml-ingestion.ts:206,208,590`, `blockchain.ts:173` y `bank-reconciliation.ts:148` embeben cualquier `err.message` en cuerpos 2xx, y no hay 404 propio (Express responde su HTML inglés). Los 72 códigos y los 24 castellanos (33 %) se sostienen, igual que la deriva `PERIOD_ALREADY_OPEN` es/en (`fiscal-calendar-service.ts:246/376`). `src/api` está en inglés al **85 %** (100 de 117 clasificables) y `src/services` en castellano al 81 %; a eso se suma texto que el censo no vio y sí llega al cliente: `ConflictoDeIdempotencia` (castellano, 409, `idempotencia.ts:372`), `NoAccessError` y el verificador OIDC (inglés, `auth.ts:134-136`), las 19 cadenas inglesas de `src/ai/draft-service.ts` (422 vía `ai.ts:97`) y los 404 derivados de nombres de tabla en `src/database/scope.ts:155`. El vocabulario de entrada (enums Zod, nombres de campo, `field:`) ya es inglés; lo castellano es la prosa, `?method=`, 11 claves de respuesta y el contrato publicado, que lleva **10** claves de extensión castellanas (no 2) acuñadas en `openapi.ts:137-157`. Lo que rompe un cambio de mensaje es más ancho que lo dicho: 766 aserciones de texto en `tests/` en el commit (772 no existe en ningún árbol; 983 con `toThrowError`), 11 frases inglesas clavadas en `withdrawn-endpoints.spec.ts:109-142`, **7 servicios emisores y `src/cli/entry-command.ts` hasheados en `manifiesto.json`** (`ci.yml:158`), `criterios.ts` anclando además `INPC_BASES_DISTINTAS` (`:4779`) y `MISSING_ROLE_ACCOUNT` (`:1995`), y 129 menciones de códigos en 33 documentos `.md` incluidas tres páginas de la wiki de usuario, el catálogo del CLI y tres manuales del agente. Los umbrales de `vitest.config.ts` están en 75-95 en el commit (92-112 en el árbol de trabajo): el informe leyó un estado intermedio de un árbol que otra sesión edita, y ésa es la causa de 4 de sus 6 archivo:línea inexactos (de 92 comprobados); los otros 2 (`openapi.ts:316-352` → 181-184; `entry-command.ts:143-149` → 141-148) son errores de cita.
