# Verificación (escéptico) · Formato y locale

Objeto: `/tmp/investigacion-idioma/inventario/formato-y-locale.md`. Árbol: `/Users/victor/projects/Accounting`, HEAD `b31e62a` (el mismo que cita el informe). Todo se recontó desde la raíz con `command grep` (BSD) y `sed -n`; no se editó ningún archivo del repositorio. Ya existía una verificación previa en esta ruta (otra sesión, 21:41); se conservó como `formato-y-locale.prev.md` y **no se copió**: cada cifra de abajo se volvió a correr. Donde la previa también se equivoca, se dice (§5).

## 0. El árbol NO está limpio, y el informe cita código sin commit

`git status --short` → 7 modificados + 4 sin seguimiento (`src/services/jurisdiccion/`, `tests/services/jurisdiccion/`, `tests/accounting/iva-ppd-reclass.spec.ts`, `tests/integration/jurisdiction-predicate.int.spec.ts`); `vitest.config.ts` con +17 sin commit (`git diff --stat HEAD -- vitest.config.ts`). `git ls-tree -r HEAD --name-only | grep jurisdiccion` → sólo `docs/jurisdicciones.md`: **`src/services/jurisdiction/jurisdiction.ts` no existe en `b31e62a`**. El informe lo presenta como «la única derivación jurisdicción→moneda del sistema» y cita su `include` en `vitest.config.ts:27` como regla vigente. Son trabajo de otra sesión en curso (memoria «Árbol compartido»): un plan puede apoyarse en ellos sólo con esa salvedad escrita.

## 1. Veredictos por conteo (tabla §2 del informe)

| # | Reclamo | Veredicto | Recuento y comando |
|---|---|---|---|
| 1 | `Intl.` en `src` = 0 | SÍ | `command grep -rn "Intl\." src \| wc -l` → 0 (todo tipo de archivo) |
| 2 | `Intl.` en docs+skills = 2 (1 real, 1 «XBRL Intl.») | SÍ | `command grep -rn "Intl\." skills docs --include='*.md'` → `ux-salida.md:414` y `conectores.md:434` |
| 3 | `toLocale*` en `src` = 8 (7 en-US, 1 es-MX) | SÍ | `command grep -rn "toLocale" src` → 8 líneas, las mismas 8 archivo:línea |
| 4 | `toLocale*` tests 0 / scripts 2 | SÍ | `command grep -rn toLocale tests \| wc -l` → 0; `scripts` → `plantilla.html:354`, `reclasificar-iva-ppd.ts:30` |
| 5 | `'es-MX'` / `'en-US'` en `src` = 2 / 7 | SÍ | `command grep -rn "es-MX" src \| wc -l` → 2; `en-US` → 7 |
| 6 | `localeCompare` en `src` = «13 en 11 archivos (+1 comentario)» | PARCIAL — cuenta doble el comentario | `command grep -rn localeCompare src --include='*.ts'` → **13 líneas incluido** el comentario `catalogo-cuentas.ts:280`; 12 líneas con llamada; `-o` → 14 (13 llamadas: `openapi.ts:83` tiene dos). Archivos: 11 con la palabra, **10** con llamada. |
| 7 | `localeCompare` scripts/tests = 1/0 | SÍ | `scripts/catalogo-estado.ts:342`; tests 0 |
| 8 | Dinero con `$` a mano = **3** de 279 | **NO — son ≥ 7** | El regex del informe exige `toFixed\|monto\|amount…` dentro de las llaves. Filtrando los marcadores SQL en vez de adivinar nombres (`command grep -rn '\$\${' src --include='*.ts' \| command grep -vE '\.length\|\+\+\|\$\$\{(i\|n\|k\|j\|idx\|pos\|param\|index\|paramIndex\|b \+)'`) quedan además: `src/cli/ai-command.ts:113` (`~$${r.costo_por_borrador_usd} USD`), `src/services/payroll/usa/benefits/benefits-service.ts:183` (`limit … $${limit}`), `src/services/payroll/usa/federal/fica-calculator.ts:86` (`over $${threshold}`), `src/services/payroll/usa/federal/futa-calculator.ts:32` (`on first $${wageBase}`). Los 279 totales sí se reproducen. |
| 9 | `'MXN'` 26/17; `'USD'` 12 | SÍ | 26 líneas, 17 archivos, 12 |
| 10 | `toFixed(` 569/104; `(2)` 197; `(4)` 187; `<const>` **151**; cli 53; tests 197 | PARCIAL | 569 líneas / 104 archivos / cli 53 / tests 197: SÍ. `toFixed(2)` 197 y `toFixed(4)` 187 son LÍNEAS; por ocurrencia (`-o`) 228 y 198, y el total por ocurrencia es **614**. El «151» no se reproduce con ningún patrón razonable: `toFixed([A-Za-z_]` → 163 líneas / 166 oc.; `toFixed([A-Z_]` (constantes en mayúscula) → 125 / 126. |
| 11 | Separador de miles a mano = 2 | SÍ | `command grep -rnE '\(\?=\(\\d\{3\}\)' src scripts --include='*.ts' --include='*.html'` → `output.ts:176`, `plantilla.html:358` |
| 12 | 12 formateadores en 11 archivos | SÍ con su regex | 12/11 reproducidos; es una lista cerrada de nombres, no un censo (`money`/`fmt` en otros nombres no se cuentan). |
| 13 | `formatMoneyMx`: 21 líneas; «8 llamadas» en diot+isn; «9 líneas de `output.spec.ts` + `presentation.spec.ts`» | PARCIAL | 21 sí. Llamadas: `diot-command.ts:550,551,562,564` + `payroll-isn-command.ts:533,1267,1432` = **7** (las otras 2 son `import`). Las 9 líneas de tests están TODAS en `presentation.spec.ts` (`command grep -rl formatMoneyMx tests` → 1 archivo); `output.spec.ts:131` es un comentario sobre es-MX, no menciona `formatMoneyMx`. |
| 14 | Plurales `palabra(s)` = 304; top 46/19/15/15/14/12 | SÍ | mismo comando → 304; `cut -d: -f1 \| sort \| uniq -c \| sort -rn` reproduce el top. Sólo 2 de las 304 son comentarios. Palabra más frecuente: `test(s)` 33, `cuenta(s)` 17, `partida(s)` 15, `account(s)` 12. |
| 15 | Plurales ternario = **9** | **NO — 18 (o 22)** | El regex del informe sólo ve `'' : 's'`. `command grep -rnE "(===\|!==\|>\|<=\|>=) 1 \? ['\"][a-záéíóúñ ]*['\"] : ['\"][a-záéíóúñ ]*['\"]" src --include='*.ts'` → **18**: además de las 9, `banner.ts:53-55` (`'draft':'drafts'`…), `doctor-service.ts:553-555`, `criterio-cierre.ts:206` (`'entry':'entries'`), `init/s5-import.ts:447` (`'y':'ies'`), `batch-service.ts:886` (`'fue reversada':'fueron reversadas'`), `rep-linkage.ts:173`. Con `(=== 1\|!== 1\|> 1\|<= 1) ?` + comilla → 22 (`criterios.ts:723`, `docs-tools.ts:186`, `statement-checks.ts:487` también). El «313» del resumen es ≥ 322. |
| 16 | Tablas de mes = 4; 170 brutas | SÍ (174 brutas hoy) | mismo comando → 174 (árbol modificado); las 4 tablas coinciden. **Pero** ver H1: hay un TERCER sitio que acuña `period_name`, sin tabla de meses. |
| 17 | `period_name` 172; 2 búsquedas por subcadena; 34 líneas de tests con `'January 2026'` | SÍ, incompleto | 172 / `close-command.ts:196`, `closing-command.ts:156` / 34. Faltan **4 líneas de tests de integración que fijan `'Periodo 3/2026'`** (H1). |
| 18 | Zona horaria: 12 líneas, 1 de código | SÍ | 12; sin comentarios queda `fiscal-calendar-service.ts:534`. Ver H4: la clase «hoy según el reloj» tiene 18 `CURRENT_DATE` que el informe no contó. |
| 19 | `TZ` en `.github`/`vitest.config` = 0 | SÍ | 0. `tests/config/env-example.spec.ts:35` sí declara `TZ` como variable del entorno reconocida. |
| 20 | getters locales 38 / `getUTC*` 27 / `Date.UTC(` 28 / `new Date(` 317 | SÍ | reproducidos |
| 21 | ISO 194 / 25 / 42 / 184 | SÍ | reproducidos |
| 22 | `DD/MM` fuera de `fecha.ts`: 3 `formatoFecha` en 4 perfiles | SÍ | `perfiles-csv.ts:72,109,148`; `AAAA-MM-DD` (forma española) en `parseo.ts:219` y `hechos.ts:93` |
| 23 | `to_char(` 34 / con nombre de mes 0 | SÍ | 34 / 0; formatos: `YYYY-MM-DD` ×33, `YYYY-MM-DD"T"HH24:MI:SSOF` ×2, `FM9999999999990.0000` ×1 |
| 24 | `COLLATE`/`lc_*` sólo `catalogo-cuentas.ts:276`; `setTypeParser` 0 en `src/database` | SÍ, incompleto | 1 / 0. `setTypeParser` aparece 5 veces en `src` como comentario que explica que NO existe. Ver H2 (ORDER BY). |
| 25 | `language` 29; consumidores de `resolveLanguage()` 4 | SÍ | 29; `system-prompt.ts:185`, `mnemosine.ts:1066,2045`, `status-command.ts:253` |
| 26 | `MNEMOSINE_LANG` src **3** / tests 6 en 3 | **NO — src 5** | `command grep -rn MNEMOSINE_LANG src --include='*.ts'` → `config.ts:1120,1126`, `mnemosine.ts:2046,2051,2053`. Tests 6/3: SÍ. Además `.env.example:352` (no citado) y **contradice al código**: dice «Omisión: la del sistema» (`:350`) y `config.ts:1131` devuelve `'es'` fijo. |
| 27 | `MNEMOSINE_LOCALE` 0/1; `Accept-Language` 0 | SÍ | 0 / `ux-salida.md:418` / 0 |
| 28 | Columnas `locale/language/idioma/lang/timezone` en migraciones = 0 | SÍ | los aciertos son `LANGUAGE plpgsql` y el comentario `029:8-13`; timezone 0 |
| 29 | Columnas de moneda = **27** con `^\s*[a-z_]*currency[a-z_]* ` | **NO con ese comando** | ese regex da **18**; 27 es `grep -i currency` a secas. La lista de `DEFAULT 'USD'` omite `001_core_schema.sql:342` (`transactions.currency_code`). |
| 30 | País/jurisdicción `001:85`, `032:24`, `008:35,124,313,335,356,377` | SÍ | reproducido |
| 31 | Tablas `tenants`/`users` = **0/0**; «`tenant_id` es columna sin tabla propia; el usuario es un sujeto OIDC» | **NO. FALSO** | El regex del informe no admite prefijo de esquema. `command grep -rnE "CREATE TABLE( IF NOT EXISTS)? (public\.)?(tenants\|users\|sessions)\b" src/database/migrations` → `001_core_schema.sql:15 CREATE TABLE public.tenants`, `:28 public.users`, `:45 public.sessions`. `tenants.settings JSONB DEFAULT '{}'` (`001:23`); `users.first_name/last_name/roles/permissions` (`001:28-43`). En `src`: 23 líneas `FROM/JOIN/INTO users`, 9 `… tenants`. Toda la §6 del informe («no hay tabla users… no hay tabla tenants») parte de este falso negativo. |
| 32 | 112 tablas en 74 migraciones | **NO — 115** | El regex sin `(public\.)?` cuenta «public» como tabla y pierde `tenants`, `users`, `sessions`, `identities`. `command grep -rhoiE "CREATE TABLE( IF NOT EXISTS)? (public\.)?[a-z_]+" src/database/migrations \| awk '{print $NF}' \| sed 's/public\.//' \| sort -u \| wc -l` → 115. 74 migraciones: SÍ; duplicados históricos 012/014/015/018 (`ls src/database/migrations \| cut -d_ -f1 \| sort \| uniq -d`). |
| 33 | Banderas de idioma/locale en el kernel = 0 | SÍ | 4 líneas (`output.ts:102`, `vocabulary.ts:136`, `riesgos-retrofit.ts:87`, `confirmacion.ts:20`), ninguna bandera |
| 34 | Variables de entorno leídas = 78 | NO — 77 | `command grep -rhoE "process\.env\.[A-Za-z_0-9]+" src --include='*.ts' \| sort -u \| wc -l` → 77; forma `process.env['X']` → 0. De idioma sólo `MNEMOSINE_LANG`: SÍ. |
| 35 | Criterios que leen archivos de la superficie = **1** (`criterios.ts:3051`) | **NO. FALSO** | El informe buscó 18 nombres a mano. Extrayendo TODAS las rutas literales de `criterios.ts` (`command grep -oE "['\"\`]src/[A-Za-z0-9_./-]+\.(ts\|json\|sql\|md)['\"\`]" src/plan/criterios.ts \| tr -d "'\"\`" \| sort -u` → 134 rutas; sólo `codigoDe/existe/rutaDe(...)` → 123) e intersecando con los archivos que el propio informe cita en §3-§4: **12** (`src/ai/budget.ts`, `src/api/rest/openapi.ts`, `src/cli/bank-command.ts`, `src/cli/kernel/riesgos-retrofit.ts`, `src/cli/mnemosine.ts`, `src/cli/report-command.ts`, `src/cli/usage-command.ts`, `src/plan/criterios.ts`, `src/services/banking/bank-account-service.ts`, `src/services/banking/treasury-posting.ts`, `src/services/policy/pending-catalog.ts`, `src/services/sat/anexo24/balanza-invariantes.ts`); con los archivos que aparecen en las filas 8 y 15 de esta tabla, **21**. Renombrar cualquiera rompe `plan:status`. |
| 36 | Umbral por archivo en `vitest.config.ts:105` para `jurisdiccion.ts` | **NO. FALSO** | `sed -n 105p vitest.config.ts` → `'src/services/reporting/report-service.ts': {`. Entradas por archivo (`command grep -nE "^\s*'src/[^']+':\s*\{" vitest.config.ts`): 92 posting, 95 validation, 98 ar-ap-posting, 101 sequence, 105 report-service, 112 criterio-cierre — **ninguna** es `jurisdiccion`; el comentario sin commit de `vitest.config.ts:26-34` dice que «NO LLEVA TRINQUETE PROPIO» a propósito (su suelo va en `SUELO_COBERTURA_UNITARIA`, `criterios.ts`). El `include` de `src/services/jurisdiccion/**` (`:27`) existe sólo en el árbol de trabajo; en HEAD el include es accounting + reporting (`git show HEAD:vitest.config.ts`). El punto 8 de §4 queda sin base. |
| 37 | `LINEA_BASE` 0 rutas de la superficie | SÍ, trivialmente | `LINEA_BASE` (`audit.ts:197-2xx`) guarda `comando\|regla\|detalle`, no rutas. Sí contiene los COMANDOS `prompt-size` (`:213`), `compact` (`:214`), `approvals list/grant` (`:215-218`) y `usage` (`:219`): los cuatro archivos con `fmt = toLocaleString('en-US')`/`fmtUsd`. Renombrar el comando (no el archivo) sí la toca. |
| 38 | Manifiesto: **1** ruta | **NO — 6** | `command grep -oE '"src/[^"]+"' src/ai/docs/manifiesto.json \| tr -d '"' \| sort -u` (49 rutas) ∩ superficie → `src/ai/budget.ts`, `src/ai/doctor-service.ts`, `src/ai/providers/config.ts` (sede de `resolveLanguage`/`AgentLanguage`), `src/cli/bank-command.ts`, `src/services/accounting/fiscal-calendar-service.ts`, `src/services/banking/statement-checks.ts`. |
| 39 | Specs = 38 archivos; conteos por identificador | SÍ con salvedad | Por identificador se reproducen (analizarFecha 26/1, analizarImporte 31/1, formatMoneyMx 9/1, nombrarPeriodo 3/1, jurisdiccionDe 12/2, monedaLegal 14/1, separadorDecimal 15/2, toLocaleString 0, localeCompare 0, `'en-US'` 0). `'es-MX'` CON comillas → 0; el «3» es sin comillas (comentarios). 38 se reproduce con una unión de 20 identificadores (`command grep -rlE "analizarFecha\|analizarImporte\|formatMoneyMx\|nombrarPeriodo\|jurisdiccionDe\|monedaLegal\|separadorDecimal\|toLocaleString\|localeCompare\|'en-US'\|'es-MX'\|es-MX\|period_name\|resolveLanguage\|MNEMOSINE_LANG\|dateOnly\|formatoFecha\|formatearImporte\|NOMBRES_DE_MES\|decidirSeparadores" tests --include='*.ts' \| wc -l` → 38), pero el informe no dio su lista. Uno de los 2 specs de `jurisdiccionDe` (`tests/services/jurisdiction/jurisdiction.spec.ts`) está sin seguimiento. |
| 40 | `nombrarPeriodo` → «3 consumidores en `inpc/`» | NO reproducible | `command grep -rn "nombrarPeriodo(" src --include='*.ts' \| grep -v "export function"` → **10** llamadas en 2 archivos (`factor.ts` 8, `inpc-service.ts` 2). |
| 41 | `dateOnly` «21 usos» | SÍ | `command grep -rn "dateOnly(" src --include='*.ts' \| wc -l` → 21 |
| 42 | `pending-catalog.ts` 134 `label:` | SÍ | `grep -c "label:"` → 134; `value:` → 134; `016_policy_decisions.sql` existe |
| 43 | «es-MX y en-US comparten separadores (1,234.56)» | SÍ (ejecutado) | Node v22.23.1, ICU 78.2, `supportedLocalesOf(['es-MX','en-US','es'])` → los tres. `1234567.891.toLocaleString('es-MX')` → `1,234,567.891` = en-US; `'es'` → `1.234.567,891`. `currency:'MXN'`: es-MX → `$1,234.50`, en-US → `MX$1,234.50`; `currency:'USD'`: es-MX → `USD 1,234.50`. Fecha 15-ene-2026: es-MX `15/1/2026`, en-US `1/15/2026`. Relevante para `policy-preview.ts:27` (pone `$` a mano y luego «MXN»). |

## 2. Muestra de 30 archivo:línea

Se abrieron con `sed -n` los 30 primeros archivo:línea de §3 (§3.1 completa, §3.2 completa, §3.3 hasta `openapi.ts:83`): `output.ts:162,155,144-145,196-197,97-109`; `cfdi-decisions.ts:46-47`; `policy-preview.ts:26-27`; `compact/usage/prompt-size:83/71/103`; `approvals-command.ts:158-159`; `usage-command.ts:72`; `budget.ts:99,109`; `report-command.ts:108-111`; `mnemosine.ts:1338`; `balanza-invariantes.ts:106`; `imss-idse-adapter.ts:104`; `payroll-isn-command.ts:1267`; `importe.ts:235-300`; `tipos.ts:129,145`; `reclasificar-iva-ppd.ts:29-30`; `plantilla.html:354-358`; `criterios.ts:5488`; `fiscal-calendar-service.ts:532-534,523`; `seed.ts:54`; `close-command.ts:196`; `closing-command.ts:156`; `periodo.ts:36-40,46-48`; `fecha.ts:19-32,148-158`; `perfiles-csv.ts:72,109,148`; `flags.ts:477-483`; `ap-controls.ts:91`; `treasury-posting.ts:583,924`; `presentation.spec.ts:16-19`; `catalogo-cuentas.ts:273-281`; `diot-service.ts:277-281`.

**30 de 30 apuntan a la línea correcta.** Se abrieron además ~30 citas de §3.4-§3.5, §4 y §5. Ahí 4 son inexactas:

1. `src/services/jurisdiction/jurisdiction.ts:159` — el informe transcribe `monedaLegal: fiscal === 'MX' ? 'MXN' : 'USD'`; la línea real es `monedaLegal: libros === 'mx_nif' ? 'MXN' : 'USD'`. La derivación es **norma contable → moneda**, no jurisdicción fiscal → moneda; y el archivo no está en HEAD.
2. `vitest.config.ts:105` — es `report-service.ts`, no `jurisdiccion.ts` (fila 36).
3. `tests/cli/kernel/output.spec.ts` como spec de `formatMoneyMx` — 0 menciones (fila 13); «8 llamadas» son 7.
4. `tests/services/sat/diot/` (§4.4: «comprobar fixtures») — esa carpeta no existe; los specs de DIOT están en `tests/sat/diot/` (`rfc`, `tercero`, `serializador`, `desglose`) y `tests/cli/diot-command.spec.ts`. Ninguno asserta el orden que produce `localeCompare` (`command grep -nE "orden|sort|localeCompare" tests/sat/diot/*.spec.ts` → nada relevante): unificar hacia unidad de código NO rompería fixtures hoy.

## 3. Hallazgos nuevos (lo que el informe no vio)

**H1 · `period_name` ya vive en DOS formatos en la misma columna, y hay un tercer sitio que la acuña.** `src/plan/conducta.ts:194` inserta en `fiscal_periods` con `` `Periodo ${m}/${ANIO}` `` («Periodo 3/2026», sin nombre de mes, en español); `tests/integration/f05d-ataque.int.spec.ts:391,521` y `tests/integration/ledger-check-filtro-esteril.int.spec.ts:75,130` lo fijan como literal (`command grep -rnE "'Periodo [0-9]" tests`). El informe dice que el campo se acuña en 2 sitios y en inglés; son 3 sitios (`fiscal-calendar-service.ts:534`, `seed.ts:54`, `conducta.ts:194`) y dos convenciones. Además la columna es `VARCHAR(50)` (`001_core_schema.sql:198`) y entra en `GROUP BY` de la vista materializada `mv_trial_balance` (`001:508,520`; `004_partitioning_and_views.sql:37,53`): derivar el nombre en render en vez de persistirlo toca también las vistas. La búsqueda `--period january` (`close-command.ts:196`) no encuentra los periodos «Periodo 3/2026».

**H2 · El orden alfabético también lo decide Postgres, y nadie fija la intercalación.** `command grep -rnE "ORDER BY [^;\`]*\b[a-z_.]*(name|nombre|code|codigo|description|descripcion|rfc|label|etiqueta|title|titulo|email)\b" src --include='*.ts' | wc -l` → **58** de 305 `ORDER BY` ordenan columnas de texto (`report-service.ts` 4, `cash-flow-service.ts` 4, `doctor-service.ts` 4, `account-service.ts` 3, `vendor-service.ts` 3…); `lower(/upper(/ILIKE/unaccent` → 60 líneas. Es exactamente lo que `catalogo-cuentas.ts:273-281` esquiva a mano para el Anexo 24. `docker/docker-compose.yml:27` levanta `postgres:15-alpine` sin `POSTGRES_INITDB_ARGS`, `LANG` ni `TZ` (`command grep -rniE "LANG|LC_|TZ|INITDB|locale" docker` → nada): la intercalación y la zona horaria del servidor son las de la imagen. El informe contó 13 `localeCompare` y dejó fuera 58 `ORDER BY` de la misma clase.

**H3 · Dos regex del propio kernel y del compactador asumen agrupación con coma.** `src/cli/kernel/output.ts:186` decide qué columna es «numérica» (y la alinea a la derecha) con `/^-?[\d,]+(\.\d+)?$/`; `src/ai/compaction.ts:249` `MONTO_RE = /(?<![\d.,])(?:\d{1,3}(?:,\d{3})+|\d{3,})\.\d{2}(?![\d])/g` rescata importes del resumen del agente sólo si llevan miles con coma y decimal con punto. Un importe «1.234,56» (convención española, la misma que el informe detecta en `criterios.ts:5488`) ni se alinea ni sobrevive a la compactación. Ninguno de los dos aparece en el inventario; son `formato_locale` de LECTURA fuera de los parsers bancarios.

**H4 · «Hoy» tiene dos relojes.** `fiscal-calendar-service.ts:523` decide el mes en curso con `now.getMonth()` (TZ del proceso Node) y **18** líneas de SQL en 8 archivos lo deciden con `CURRENT_DATE` del servidor Postgres (`command grep -rnE "\bCURRENT_DATE\b" src --include='*.ts' --include='*.sql'`: `close-service.ts`, `wake-gate.ts`, `pending-service.ts`, `close-explain.ts`, `fiscal-calendar-service.ts`, `period-close.ts`, `book-items.ts`, `reconciling-items.ts`); `NOW()` 305. Con el contenedor sin `TZ` (H2) y el proceso en CDMX, «vencido» y «mes actual» pueden discrepar unas horas al día. El informe cuenta «12 líneas de zona horaria», todas del lado Node.

**H5 · `.env.example:350-352` documenta `MNEMOSINE_LANG` con una omisión que el código no tiene.** Dice «Omisión: la del sistema»; `config.ts:1131` devuelve `'es'` fijo y nunca lee `LANG`/`LC_*` (fila 34). Es la única línea de documentación de la variable fuera de `docs/` y contradice al código.

**H6 · Normalización Unicode: existe, pero sólo en la frontera de entrada de algunos parsers.** `command grep -rn '\.normalize(' src --include='*.ts'` → 10 líneas en **7** archivos: `mnemosine.ts:1422,2638,3121`, `completion-command.ts:92,97`, `kernel/confirmacion.ts:32`, `inpc/parseo.ts:67`, `parsers/fecha.ts:209`, `parsers/texto.ts:102`, `parsers/importe.ts:61` (NFKC). Ninguna en el renderizador: `output.ts:200` mide el ancho de columna con `.length` (unidades UTF-16). `docs/auditorias/2026-09-01-usabilidad/ux-idioma.md:595-599` afirma «cero llamadas a normalize en src» — hoy es falso, y la verificación previa también se equivocó al decir que sólo `mnemosine.ts` y `completion-command.ts` normalizan.

**H7 · Documentación acoplada por nombre: 9 documentos, no 5.** `command grep -rlE "formatMoneyMx|kernel/output\.ts|resolveLanguage|MNEMOSINE_LANG|period_name|dateOnly" docs src/ai/docs README.md CONTRIBUTING.md AGENTS.md skills --include='*.md'` → `SINTESIS-brechas.md`, `flujos-reales.md`, `ux-descubribilidad.md`, `ux-errores.md`, `ux-idioma.md`, `ux-salida.md`, `completion-plan.md`, `wiki/Arquitectura.md`, `wiki/Manual-Trabajar-con-el-agente.md`. El informe sólo enlaza `ux-salida.md` y `Arquitectura.md`; `ux-idioma.md` (917 líneas) ya cubre `period_name` (brecha 6, `:373-399`), `LANGUAGE_LINE` (brecha 14, `:785-813`) y «lo que NO conviene traducir» (`:874`).

**H8 · Umbrales de integración y suelo congelado que el informe no menciona.** `vitest.integration.config.ts:53-101` tiene su propio `include` y umbrales por archivo (`period-close.ts:60`, `ledger-checks.ts:64`, `posting`, `ar-ap-posting`, `validation`, `iva-cash-basis.ts:85`, `report-service.ts:93`, `criterio-cierre.ts:97`, `cash-flow-service.ts:101`); `period-close.ts` lleva 15 plurales a mano y `criterio-cierre.ts:206` un ternario. `SUELO_COBERTURA_UNITARIA` en `criterios.ts` congela las mismas rutas. Renombrar cualquiera de esos archivos descoloca dos configs y un criterio, no una.

**H9 · No hay biblioteca que sustituir.** `package.json` sólo trae `commander`, `decimal.js`, `pg` (`command grep -nE '"(date-fns|dayjs|luxon|moment|numeral|full-icu|intl|i18next|decimal\.js|pg|commander)"' package.json`); `Decimal.set(`/`toFormat(` 0; `padStart(2, '0')` en 32 líneas / 19 archivos arma fechas a mano; `engines.node >= 20` (`:85`) garantiza full-icu, así que el riesgo «small-icu» del §4.5 del informe no aplica a las versiones soportadas.

**H10 · Menor, para el registro.** `dist/` (ignorado por git, build del 24-ago) contiene `dist/cli/init/s1-identidad.js:143` con `toLocaleString('es-MX', { month: 'long' })`: un acuñador de periodos EN ESPAÑOL que nunca llegó a `git log` (`git log -S"month: 'long'" -- src/cli/init` → nada). No afecta al plan, pero muestra que la decisión «inglés persistido» ya se cambió una vez sin dejar rastro en la historia.

Nada de lo leído contenía instrucciones dirigidas al verificador.

## 4. Lo que sí se sostiene y conviene no perder

Las cifras mecánicas del núcleo son exactas: 0 `Intl.`, 8 `toLocaleString` (7 en-US / 1 es-MX), 569 líneas `toFixed`, 304 plurales `palabra(s)`, 172 `period_name`, 34 tests con `'January 2026'`, 12 líneas de TZ, 194/25/42/184 de fechas ISO, 34/0 `to_char`, 0 columnas de idioma/locale/timezone, 0 `--locale`, 0 `Accept-Language`, 77 variables de entorno con una sola de idioma. El contrato «locale sólo en `table`» (`presentation.spec.ts:10-14`, `output.ts:193-195`), la regla del Anexo 24 (`catalogo-cuentas.ts:273-281`) y la contradicción `functional_currency DEFAULT 'USD'` (`001:86`) contra «ante la duda, mexicana» están bien citados.

## 5. Sobre la verificación previa (`formato-y-locale.prev.md`)

Coincide en las refutaciones grandes (tenants/users, 115 tablas, criterios ≥ 10, manifiesto ≥ 5, vitest:105, ternarios 18, `$` a mano ≥ 7). Dos cosas suyas no se sostienen: (a) H3 dice que `.normalize(` vive «todas en `mnemosine.ts` y `completion-command.ts`, ninguna en los parsers» — son 7 archivos y 4 de ellos SON parsers (H6); (b) no vio `usage` en `LINEA_BASE:219` ni el tercer acuñador de `period_name` (`conducta.ts:194`), ni `CURRENT_DATE`, ni las dos regex de agrupación con coma (H3).

## 6. Resumen corregido

El informe acierta en las cifras que salen de un `grep` directo y en sus archivo:línea (30/30 en la muestra), y falla sistemáticamente donde construyó regex a mano para medir ACOPLAMIENTO: hay tablas `tenants` (con `settings JSONB`) y `users` (`001:15,23,28`), no «0/0»; `criterios.ts` lee por ruta 12 de los archivos que el propio informe cita (21 con los de las filas 8 y 15), no 1; el manifiesto hashea 6, no 1; `vitest.config.ts:105` es `report-service.ts` y `jurisdiccion.ts` ni tiene umbral ni existe en HEAD; `MNEMOSINE_LANG` está en 5 líneas de `src`, no 3; los ternarios son 18-22, no 9; el dinero con `$` a mano ≥ 7, no 3; las tablas 115, no 112. Lo que no vio: `period_name` ya convive en inglés («January 2026») y en español-numérico («Periodo 3/2026», `conducta.ts:194`, fijado por 4 tests de integración) y entra en el `GROUP BY` de una vista materializada; 58 `ORDER BY` sobre texto + 60 `lower/ILIKE` cuya intercalación decide un `postgres:15-alpine` sin locale ni `TZ`; 18 `CURRENT_DATE` que hacen que «hoy» tenga dos relojes; dos regex (`output.ts:186`, `compaction.ts:249`) que sólo entienden miles con coma; `.env.example:350` prometiendo una omisión «del sistema» que el código no implementa; y `ux-idioma.md`, que ya había hecho la mitad del inventario. La conclusión «acoplamiento externo bajo» cae: es medio y está concentrado en `criterios.ts`, el manifiesto, los dos `vitest.*.config.ts` y las vistas del esquema.
